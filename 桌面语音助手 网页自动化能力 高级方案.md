# 桌面语音助手 网页自动化能力 高级方案

## 一、方案定位与设计原则

### 1.1 能力定位

基于现有系统已有的「浏览器带调试端口启动」能力，新增**自然语言驱动的网页自动化操作**：用户通过语音下达页面操作指令（如 “点击搜索按钮”“往下翻三页”“把这个商品加入购物车”“提取页面所有标题”），系统自动解析页面、定位元素、执行操作并反馈结果，实现 “说话就能控制网页” 的全链路语音交互。

该能力完全复用现有 Chrome/Edge 调试端口、通义千问视觉大模型、LangGraph 智能体框架，无需引入重型驱动，与现有浏览器管理能力深度打通，属于现有浏览器工具集的高阶增强。

### 1.2 设计原则

- **资产最大化复用**：复用现有 `open_browser` 的调试端口能力、GLM-4.1V-Thinking-Flash视觉模型、LangGraph ReAct 智能体、语音播报链路，不重复造轮子。
- **架构零侵入**：以新增工具集 + 专项服务的形式接入，不改动现有 UI、桥接层、智能体核心逻辑，无功能倒退风险。
- **自然语言友好**：支持模糊口语化指令，不要求用户记忆固定命令，由大模型完成 “语义 → 页面操作” 的映射。
- **安全可控**：内置多层安全防护，限制操作范围、拦截高危行为、敏感操作二次确认，避免误操作造成损失。
- **稳定容错**：支持元素定位失败重试、页面加载等待、异常自动降级，确保复杂页面下的操作成功率。

## 二、整体架构设计

### 2.1 在现有系统中的位置

新增能力集中在**服务层 + 工具层**，属于现有浏览器工具分类的扩展，上层智能体与交互层完全复用现有架构。

表格

|   层级   | 改造 / 新增 |                             说明                             |
| :------: | :---------: | :----------------------------------------------------------: |
|  UI 层   |  最小增强   |       调试面板新增网页操作日志，悬浮球状态复用现有四态       |
|  桥接层  |   无改造    |           任务打断、代际守卫、状态机逻辑 100% 复用           |
| 智能体层 |  工具绑定   |        新增网页自动化工具集，纳入 Agent 工具调度范围         |
|  服务层  |  新增服务   | 新增 `BrowserAutomationService`，封装 CDP 连接、页面解析、元素定位、操作执行 |
| 基础设施 |   全复用    | 复用浏览器调试端口、GLM-4.1V-Thinking-Flash 视觉模型、现有音频链路 |

### 2.2 核心模块流转逻辑

```
用户语音指令 → ASR识别 → Agent理解意图
    ↓
调用页面分析能力 → 提取DOM结构/页面截图 → 大模型匹配目标元素
    ↓
生成操作序列 → 原子化执行（点击/输入/滚动/提取）
    ↓
操作结果校验 → 语音/面板反馈 → 任务结束/继续下一步
```

完整执行闭环：

1. **意图理解**：Agent 解析用户指令，判断是网页操作类需求，调用对应工具；
2. **页面感知**：通过 Chrome DevTools Protocol (CDP) 获取当前页面的 DOM 结构、可交互元素列表，或截取页面完整截图；
3. **元素定位**：结合 DOM 语义解析 + 视觉大模型，匹配用户描述的目标元素，得到精准定位；
4. **操作执行**：调用 CDP 接口执行原子操作，全程不依赖模拟鼠标键盘，不占用屏幕焦点；
5. **结果反馈**：校验操作是否成功，提取结果文本，通过语音播报给用户，复杂操作可分步反馈。

## 三、核心能力体系

### 3.1 基础导航类操作

覆盖页面基础浏览动作，替代手动点击地址栏、前进后退等操作，全语音可控：

- 页面导航：刷新、前进、后退、跳转到指定网址、停止加载
- 页面滚动：向上 / 向下翻页、滚动到顶部 / 底部、滚动到指定元素位置
- 标签管理：切换到指定标题的标签页、新建标签页、关闭当前标签（复用现有关闭能力）
- 缩放控制：页面放大 / 缩小、恢复默认缩放比例

### 3.2 元素交互类操作

核心能力，支持对页面内可交互元素进行精准操作，覆盖绝大多数网页交互场景：

- **点击操作**：点击按钮、链接、复选框、单选框、下拉菜单等，支持 “第几个 xx”“红色的按钮”“标题为 xxx 的卡片” 等模糊描述
- **输入操作**：在输入框、搜索框、表单中填写指定内容，支持清空后输入、追加输入两种模式
- **选择操作**：勾选 / 取消勾选复选框、选中下拉菜单指定选项、切换单选框
- **悬浮操作**：鼠标悬浮到指定元素上，触发悬浮菜单 / 提示框
- **拖拽操作**：将元素 A 拖拽到元素 B 位置（适用于排序、滑块调节等场景）

### 3.3 信息提取类操作

结合大模型能力，从页面中提取用户需要的信息，语音播报结果，实现 “看网页” 能力：

- 文本提取：提取指定区域的文字内容、提取页面所有标题、提取表格数据
- 状态查询：查询当前页面标题、网址、加载状态、是否有未读消息
- 内容总结：对当前页面的文章、商品详情、搜索结果进行总结，提炼核心信息后播报
- 元素查找：判断页面上是否存在指定内容 / 元素，返回位置与状态

### 3.4 流程自动化类操作

支持多步骤连续操作，由 Agent 自主规划执行路径，完成复合任务：

- 搜索类流程：“在百度搜索今天的天气” → 自动打开网站 → 定位搜索框 → 输入关键词 → 点击搜索 → 提取结果播报
- 表单类流程：“在这个页面填写我的收货地址” → 自动定位各个输入框 → 依次填入预设信息
- 对比类流程：“把这页的商品名称和价格整理出来” → 遍历商品列表 → 提取结构化信息 → 总结播报

## 四、关键技术机制

### 4.1 双引擎元素智能定位

解决 “用户说自然语言，系统怎么找到页面对应按钮” 的核心问题，采用「DOM 语义引擎 + 视觉定位引擎」双校验模式，兼顾速度与准确率。

#### 引擎一：DOM 语义定位（优先，速度快，零额外开销）

1. 通过 CDP 提取当前页面所有可交互元素（按钮、输入框、链接、菜单等），生成结构化元素列表；
2. 每个元素附带：文本内容、aria-label 描述、id/class 属性、元素类型、在页面中的位置序号、可见性状态；
3. 将元素列表整理为自然语言格式，送入大模型，由 LLM 根据用户指令语义，匹配最符合描述的元素，返回其唯一选择器。

#### 引擎二：视觉辅助定位（兜底，复杂页面适用）

1. 截取当前页面完整截图，标注元素坐标网格；
2. 将截图 + 用户指令送入 qwen-vl-max 视觉大模型，直接识别目标元素的像素坐标与边界框；
3. 结合 DOM 引擎结果交叉校验，确保定位准确，适配无文本的图标按钮、图片按钮等特殊元素。

#### 模糊匹配策略

- 支持序号描述：“第三个搜索结果”“最上面的按钮”
- 支持特征描述：“蓝色的提交按钮”“带购物车图标的入口”
- 支持语义描述：“返回上一页的按钮”“关闭弹窗的叉号”
- 匹配失败时自动降级：提示用户补充描述，或列出可选元素让用户确认

### 4.2 原子化操作执行层

所有网页操作拆解为原子指令，通过 CDP 协议直接下发到浏览器内核执行，相比模拟鼠标键盘具备三大优势：

- **不占用焦点**：操作时浏览器可后台运行，不影响用户当前桌面操作，不会误点其他窗口；
- **稳定性高**：不受屏幕分辨率、窗口位置、缩放比例影响，元素定位精准无偏差；
- **效率更快**：直接操作 DOM，跳过系统鼠标事件队列，操作响应延迟 < 100ms；
- **可感知结果**：操作后可立即获取元素状态、页面变化，自动判断是否执行成功。

### 4.3 智能规划与分步执行

针对多步骤复合任务，复用现有 LangGraph ReAct 循环，由 Agent 自主拆解与执行：

1. 用户下达复合指令后，Agent 先分析任务目标，拆解为有序操作步骤；
2. 每执行一步操作，自动校验页面状态变化，确认成功后再执行下一步；
3. 遇到异常（元素找不到、页面加载失败）自动重试，最多重试 2 次，仍失败则向用户反馈问题；
4. 关键步骤可通过语音同步反馈，比如 “已经输入关键词，正在搜索”，让用户感知进度。

### 4.4 多层级安全防护体系

网页自动化属于高危操作，设置四道安全防线，避免误操作与风险：

1. **操作范围限制**：仅支持对「助手启动的带调试端口的浏览器实例」进行操作，用户手动打开的浏览器无权限，避免误操作私人页面；
2. **域名白名单机制**：默认仅允许在常用站点（搜索、资讯、办公等）执行完整操作，陌生域名下仅允许浏览类操作，禁止输入、提交等高危动作；
3. **高危操作二次确认**：涉及表单提交、订单支付、账号登录、删除内容等操作，自动暂停并语音询问 “确认要提交吗？”，用户确认后才执行；
4. **敏感信息拦截**：禁止自动输入密码、验证码、身份证号、银行卡号等敏感信息，检测到相关指令直接拒绝并提示风险；
5. **操作可回溯**：所有操作写入历史记录，支持 “撤销上一步”，通过页面后退、恢复标签等方式回退操作。

## 五、与现有系统的无缝集成

### 5.1 与现有浏览器工具集成

- 复用 `open_browser` 的调试端口启动逻辑，浏览器启动后自动建立 CDP 长连接，无需重复连接；
- 复用现有标签页管理、关闭、恢复能力，新旧工具共享同一份浏览器实例数据；
- 延续现有降级策略：当浏览器无调试端口时，降级为模拟快捷键操作，并提示用户重启浏览器以获得完整能力。

### 5.2 与智能体层集成

- 将网页自动化工具集纳入现有 Agent 工具列表，由大模型自主判断调用时机，无需用户指定具体工具名；
- 复用现有 SQLite Checkpoint 记忆能力，支持跨轮次网页操作，比如 “刚才那个页面再往下翻一点”；
- 复用高危操作确认机制，与现有文件删除的二次确认逻辑统一，交互体验一致。

### 5.3 与语音交互链路集成

- 操作过程全程支持语音打断，复用现有任务打断状态机，执行网页操作时可随时用语音中止或切换任务；
- 操作结果自动语音播报，简短操作一句话反馈，复杂结果提炼核心信息播报，详情可在面板查看；
- 复用回声防护逻辑，语音播报期间不接收页面操作指令，避免误触发。

### 5.4 与 UI 层集成

- 调试面板新增「网页操作」日志分区，实时显示每一步操作、元素定位结果、执行状态，方便调试；
- 悬浮球状态复用现有四态，操作中保持 THINKING 状态，无需新增状态；
- 设置中心新增网页自动化专属配置页，可开关安全策略、调整重试次数、管理白名单域名。

## 六、三级落地路径

### 6.1 基础版：核心原子操作（落地周期 1 周）

**核心交付**：CDP 连接管理 + DOM 语义定位 + 8 个高频原子操作，满足基础网页控制需求。

- 实现内容：
  1. 搭建 BrowserAutomationService，复用现有调试端口，建立稳定 CDP 连接；
  2. 实现 DOM 元素提取与语义匹配定位引擎；
  3. 上线第一批次工具：页面刷新 / 前进 / 后退、上下滚动、点击元素、输入文本、提取页面文本、切换标签页；
  4. 接入基础安全策略：操作范围限制、陌生域名输入拦截。
- 收益：覆盖 80% 日常网页操作场景，改造成本低，稳定性高，快速补齐核心能力。

### 6.2 进阶版：视觉增强 + 模糊指令（落地周期 2 周）

在基础版之上，补齐定位能力与交互体验，达到商用级可用性。

- 实现内容：
  1. 接入视觉定位引擎，复用 qwen-vl-max，实现图标、图片等无文本元素的定位；
  2. 支持模糊描述、序号描述、特征描述等多种口语化指令；
  3. 新增复选框操作、下拉选择、悬浮、拖拽等进阶交互工具；
  4. 新增页面内容总结、结构化信息提取能力；
  5. 完善异常重试、页面加载等待、弹窗自动处理机制。
- 收益：复杂页面操作成功率大幅提升，指令自由度高，接近 “说什么就能做什么” 的体验。

### 6.3 高级版：流程自动化 + 自愈能力（落地周期 3~4 周）

从单步操作升级为全流程自动化，具备自主规划与异常修复能力。

- 实现内容：
  1. 基于 LangGraph 实现多步任务自主规划，支持搜索、填表、信息整理等复合流程；
  2. 新增操作自愈能力：元素找不到时自动滚动页面、切换 iframe、关闭弹窗后重试；
  3. 支持常用站点预设模板，针对电商、搜索、办公等场景优化指令识别与操作路径；
  4. 新增操作录制与回放功能，可保存常用操作流程，一句话触发执行；
  5. 完善安全审计与白名单管理，支持自定义安全规则。
- 收益：实现真正的 “语音管家” 级网页自动化，复杂任务一句话交付，系统自主完成全流程。

## 七、边界场景与异常处理

1. **页面加载缓慢**：设置默认页面加载等待时长，支持动态等待目标元素出现，超时后提示用户 “页面加载较慢，请稍后再试”；
2. **元素定位失败**：先自动重试 2 次（切换定位引擎、滚动页面、关闭弹窗），仍失败则列出页面可见元素供用户确认，或请用户重新描述；
3. **弹窗 / 广告遮挡**：自动检测常见弹窗、广告关闭按钮，优先尝试关闭后再执行操作；无法自动关闭则提示用户；
4. **iframe 嵌套页面**：自动识别页面内的 iframe，逐层切入查找元素，无需用户干预；
5. **动态渲染页面**：采用轮询 + 事件监听结合的方式，等待元素渲染完成后再执行操作，避免点击无效；
6. **浏览器意外关闭**：自动检测连接状态，断开后语音提示 “浏览器已关闭”，清理连接资源，不影响系统其他功能。

## 八、可选体验增强项

1. **元素高亮反馈**：定位到目标元素后，在页面上用彩色边框短暂高亮，用户可直观看到要操作的位置，确认无误再执行；
2. **操作步骤语音引导**：复杂多步任务，每执行一步前语音播报 “接下来我要点击搜索按钮了”，用户可随时叫停；
3. **常用站点指令模板**：针对抖音、百度、电商网站等高频站点，内置优化指令集，识别更准、执行更快；
4. **快捷指令收藏**：支持用户收藏常用操作流程，比如 “打开每日热搜”，一句话直接执行完整流程。