# 桌面语音助手 — 项目文件结构与作用说明

> **生成时间**：2026-08-11
> **项目根目录**：`D:\zhuomZNT`
> **说明**：本文档对仓库根目录及主要子目录中的所有文件进行扫描，逐一说明其作用、所属层级与关键职责。构建产物（`venv_assistant/`、`dist/`、`build/`、`__pycache__/`、`.git/`）不在归档范围内，仅在末尾简要提及。

---

## 一、项目概览

这是一个**本地桌面语音助手**项目（PySide6 GUI + LangGraph Agent + 千问 LLM），具备：

- 语音唤醒 / 持续监听 / 语音识别（SenseVoice 离线 + DashScope 云端 ASR）
- 扬声器直接播报（Edge-TTS，不走媒体播放器）
- 自然语言指令 → 工具调用（打开网页/应用、读写文件、搜索新闻、网页自动化等）
- 屏幕监控 / 桌面应用识别 / 截图视觉问答（GLM-4.1V）
- 两层记忆（会话内续接 + 跨会话长期记忆）
- 完整网页自动化（CDP 控制浏览器、点击/输入/抓文本/管标签）

代码按 `core / infra / services / tools / ui / utils` 分层，业务逻辑下沉、入口精简。

---

## 二、根目录文件（顶层）

| 文件 / 目录 | 类型 | 作用 |
|---|---|---|
| **`启动语音助手.bat`** | 脚本 | 一键启动器。强制用项目 venv 的 `python.exe` 跑 `src/main.py`，避免误用系统 Python（曾因 Anaconda 无 PySide6 导致启动即崩）。双击即用。 |
| **`Assistant.spec`** | 打包配置 | PyInstaller 打包脚本。顶部两个开关：`DEVELOP_MODE`（是否带命令行黑框）、`ONE_FILE`（单文件/目录模式）。已排除 torch 等离线依赖以瘦身。 |
| **`.env`** | 配置 | 所有密钥与运行时参数集中处：Qwen LLM（`QWEN_API_KEY`/`QWEN_MODEL`）、视觉模型 GLM-4.1V（`GLM_API_KEY`/`GLM_VL_MODEL`）、ASR（`DASHSCOPE_API_KEY`）、TTS 引擎与嗓音、热键、唤醒词、工作目录等。**不会进 git**。 |
| **`.gitignore`** | 配置 | 忽略 venv、`data/`、构建产物、`*.db`、`.env`、临时文件等。注意：其中 `*.spec` 被忽略，但当前 `Assistant.spec` 仍以文件形式存在（未提交）。 |
| **`ruff.toml`** | 配置 | Ruff 代码质量门禁：仅卡 E/W/F 级错误，行宽 120。`src/main.py` 等刻意 `import` 顺序（torch 先于 PySide6）被豁免 E402。 |
| **`requirements.lock.M0.txt`** | 依赖锁 | M0 阶段依赖快照（基础库：aiohttp、anyio、langchain 等）。 |
| **`requirements.lock.M6.txt`** | 依赖锁 | M6 阶段依赖快照（在 M0 基础上增加 aliyun-sdk、pycaw、comtypes、Pillow 等）。 |
| **`_check_audio_deps.py`** | 辅助脚本 | 校验 4 个音频依赖（sounddevice / numpy / soundfile / pyaudio）能否 import。调试用。 |
| **`_check_m6_deps.py`** | 辅助脚本 | 校验 M6 新增 4 依赖（PyInstaller / pycaw / comtypes / Pillow）是否安装成功。 |
| **`_probe_pycaw.py`** | 辅助脚本 | 探究 pycaw 音频音量控制 API 的用法（AudioUtilities 接口探测）。开发期排错遗留。 |
| **`_render_ui_preview.py`** | 辅助脚本 | Headless（offscreen）把三大 UI 组件渲染成 PNG（ball/panel/settings.png），输出到 `data/ui_preview/`。无需显示器即可预览 UI。 |
| **`assets/`** | 目录 | 静态资源占位目录（当前为空，图标等未来放这里）。 |
| **`trae/`** | 目录 | Trae IDE 相关（`documents/` 当前为空，用于存放编辑器导出的文档）。 |
| **桌面语音助手-分步执行计划.md** | 文档 | 原始分阶段开发 Playbook（M0~M9 实施路线图）。 |
| **桌面语音助手-系统现状总结.md** | 文档 | 2026-08-10 生成的系统功能/架构/已知限制总结报告。 |
| **桌面语音助手 网页自动化能力 高级方案.md** | 文档 | 网页自动化（M9）能力的专项设计方案文档。 |
| **`.workbuddy/`** | 目录 | 本助手（WorkBuddy）的工作记忆目录（项目级 memory 与日志）。 |

---

## 三、源代码 `src/`（核心）

### 3.1 入口与基础设施

| 文件 | 行数 | 作用 |
|---|---|---|
| **`src/main.py`** | 196 | **程序唯一启动入口**，刻意保持精简。职责：① 文件顶部预加载 torch/funasr 等原生库（防 PySide6 DLL 段错误）；② 加载 `.env`（`override=True` 防系统环境变量污染）；③ 初始化 SQLite 表与 Checkpoint；④ 构造 `AppController`/`UIBridgeService` 并启动 Qt 事件循环。**业务逻辑全部下沉**。 |
| **`src/core/state.py`** | 98 | LangGraph `AgentState` 状态定义（messages / thread_id / pending_confirm / runtime_system / step_count 等）。是 Agent 图的数据契约。 |
| **`src/infra/llm_client.py`** | 74 | LLM 客户端封装。**主对话模型固定用通义千问 Qwen**（`get_qwen_llm()`，`qwen-plus`）。视觉模型（GLM-4.1V / Qwen-VL）不在此处，由各工具直接调用。 |

### 3.2 服务层 `src/services/`（业务核心）

| 文件 | 行数 | 作用 |
|---|---|---|
| **`agent_service.py`** | 590 | **中枢**：把 16 个工具 + LLM + Checkpointer 组装成 ReAct Agent（LangGraph）。持有系统提示词（含「必须调工具」「禁止格式化输出」等规则）、消息窗口截断、长期记忆注入（`runtime_system`）、对话执行与状态查询。 |
| **`ui_bridge_service.py`** | 900 | **UI ↔ 业务桥接层**：信号槽 + QThread 异步。接收文本/语音提交→调度 Agent→emit 气泡与 TTS；职责含：固定主会话 `main`（记忆连续）、任务打断二次确认状态机、回复清理（`clean_response` 过滤 Markdown/Emoji）、后台记忆提炼、会话切换（`set_active_thread`）。 |
| **`asr_service.py`** | 383 | 语音识别服务。SenseVoice（FunASR + ModelScope）离线识别为主，DashScope 云端 ASR 兜底；含 VAD 切段、录音设备管理。 |
| **`tts_service.py`** | 454 | 语音播报服务。双后端：Edge-TTS 在线（默认，扬声器进程内播放）、CosyVoice 本地离线占位；支持 `stop_audio_playback()` 掐断。 |
| **`wake_word_service.py`** | 779 | 唤醒词常驻监听 + 持续监听（M8 进阶）。能量 VAD + 实时转写预览 + 收尾词 + 追加合并 + 语义断句；唤醒后自动暂停/恢复监听。 |
| **`memory_service.py`** | 233 | **长期记忆服务**：每轮对话后用 LLM 提炼「偏好/任务/事实」存入 SQLite `memories` 表；下次对话前检索注入，让 LLM 真正"记得之前做过什么"。 |
| **`checkpoint_service.py`** | 254 | LangGraph Checkpoint 服务：按环境选后端（SQLite Saver 持久化 / 内存 Saver），持有单例。实现对话历史跨重启。 |
| **`sqlite_db.py`** | 177 | 统一 SQLite 服务：应用配置（`settings`）、对话历史（`history`）、Checkpoint（`langgraph_checkpoints`）、长期记忆（`memories`）全写一个库 `data/app.db`。 |
| **`semantic_segment_service.py`** | 291 | M8 语义级智能断句：LLM 判断语音识别文本的语义完整性（用户是否说完），避免误切段。 |
| **`browser_automation_service.py`** | 831 | 浏览器自动化引擎：通过 Chrome DevTools Protocol (CDP) 控制带调试端口的浏览器，捕获 DOM 元素、执行点击/输入/滚动/标签管理。 |
| **`app_catalog_service.py`** | 246 | 本机应用目录服务：扫描桌面/开始菜单 `.lnk`，构建「应用名 → 可启动目标」目录（~140 应用），供语音"打开微信"等指令匹配。 |

### 3.3 工具层 `src/tools/`（Agent 可调用的能力）

| 文件 | 行数 | 作用 |
|---|---|---|
| **`__init__.py`** | 104 | 工具包统一出口：`get_all_tools()` 注册全部 16 个工具供 Agent 绑定。 |
| **`browser_tools.py`** | 1125 | 打开/关闭浏览器工具：`open_browser`（口语化站点名/URL/关键词）、`close_browser_tab`（4 模式关标签）、`restore_closed_tab`。含 CDP 启动与残留 Chrome 清理。 |
| **`file_tools.py`** | 948 | 文件操作工具：创建/搜索/删除文件（`create_file` / `search_files` / `delete_file` 带 dry_run 二次确认），白名单安全沙箱。 |
| **`web_automation_tools.py`** | 687 | 网页自动化工具集（10 个）：`browser_navigate` / `click` / `input` / `extract_text` / `list_elements` / `scroll` / `list_tabs` 等，配合视觉模型（GLM-4.1V）定位元素。 |
| **`news_tools.py`** | 460 | 新闻资讯搜索工具。 |
| **`system_tools.py`** | 423 | M6.2 系统级工具集（4 个）：系统信息、进程、音量控制等。 |
| **`voice_tools.py`** | 321 | 语音工具：直接触发 TTS 播报（`tts_speak`）等。 |
| **`app_tools.py`** | 346 | 桌面应用工具集：`open_app`（打开本机应用）、`list_active_apps`、`recognize_screen`（截图 + GLM-4.1V 视觉问答，缺时回退 Qwen-VL）。 |

### 3.4 UI 层 `src/ui/`（PySide6 界面）

| 文件 | 行数 | 作用 |
|---|---|---|
| **`application.py`** | 310 | **编排层** `AppController`：把悬浮球、主面板、设置、调试面板、历史 Tab 拼装起来，连接 UI 信号与 `UIBridgeService`。 |
| **`floating_ball_widget.py`** | 322 | P-01 桌面悬浮球：无边框、始终置顶、可拖拽、4 状态自绘动画（IDLE/LISTENING/SPEAKING/THINKING）。 |
| **`drawer_main_panel.py`** | 466 | P-02 抽屉式主对话面板：右侧滑入滑出，对话/历史/调试 3 Tab + 底部输入栏。 |
| **`settings_window.py`** | 477 | P-04 设置中心：左侧导航 Chip + 右侧 5 页（通用/语音/快捷键/AI Key/隐私）。 |
| **`history_tab.py`** | 350 | P-05 历史会话 Tab：左侧日期分组列表 + 右侧气泡回放，"继续会话"接 `set_active_thread`。 |
| **`debug_panel.py`** | 188 | P-03 调试面板：分 5 个折叠区 + 全链路彩色日志，便于开发排查。 |
| **`confirm_dialog.py`** | 221 | P-07 高危操作二次确认 Dialog：DELETE 类操作需文字二次确认。 |
| **`widgets/common.py`** | 572 | 公共控件库：Accordion / ChipButton / BubbleList / SwitchToggle / LogHighlighter 等可复用组件。 |
| **`widgets/__init__.py`** | 18 | 公共控件统一导出（缩短 import 路径）。 |

### 3.5 工具函数 `src/utils/`

| 文件 | 行数 | 作用 |
|---|---|---|
| **`path_utils.py`** | 154 | 统一路径工具：所有路径（工作目录、缓存、白名单根）从此取，禁止硬编码本机绝对路径；含 `ensure_env_file()`（exe 首次启动复制 `.env`）。 |

---

## 四、运行时数据 `data/`（自动生成，不入库）

| 路径 | 作用 |
|---|---|
| `data/app.db` (+ `-shm`/`-wal`) | 主 SQLite 库：settings / history / checkpoints / memories。 |
| `data/app_catalog.json` | 扫描生成的本机应用目录缓存（~140 应用）。 |
| `data/asr_cache/` | 麦克风录音 WAV 缓存（每次语音输入一段）。 |
| `data/tts_cache/` | TTS 合成音频缓存。 |
| `data/browser_automation_profile/` | 自动化 Chrome 的 `--user-data-dir` 配置目录。 |
| `data/checkpoints/`、`data/history/` | 旧版对话持久化目录（现统一迁入 `app.db`）。 |
| `data/M1_test_*/` | 早期 M1 文件操作测试的临时产物（沙箱）。 |
| `data/model_cache/` | 离线模型缓存（SenseVoice 等，若启用）。 |
| `data/ocr_temp/`、`data/whoosh_index/`、`data/ui_preview/` | OCR 临时、全文索引、UI 预览图。 |
| `data/*.log` | 复现/冒烟测试日志（`_repro5_*`、`_smoke_*`）。 |

---

## 五、测试 `tests/`

| 分类 | 文件 | 说明 |
|---|---|---|
| 单测 | `test_M0_skeleton.py` ~ `test_M9_web_automation.py` | 各里程碑（M0~M9）功能验证 |
| 单测 | `test_clean_response.py` | 回复清理函数验证（Markdown/Emoji 过滤） |
| 单测 | `test_memory_integration.py` | 两层记忆方案端到端验证 |
| 单测 | `test_task_switch.py` / `test_app_catalog.py` / `test_app_tools.py` / `test_browser_close_enhanced.py` / `test_continuous_listen.py` / `test_M15_tools.py` | 各模块专项测试 |
| 端到端 | `e2e_M1_*.py` | 真实/模拟 LangGraph 链路 e2e |
| 探针 | `probe_qwen_live.py` / `probe_qwen_v1_models.py` | 千问 API 联通性/模型列表探针 |

---

## 六、构建产物（不入库，仅说明）

| 路径 | 说明 |
|---|---|
| `venv_assistant/` | 项目专属 Python 虚拟环境（含 PySide6 / LangChain / LangGraph / PyInstaller 等）。 |
| `dist/` | PyInstaller 输出（`Assistant/` 目录版 或 `桌面语音助手.exe` 单文件版），内含 `.env` 副本与 `_internal/` 资源。 |
| `build/` | PyInstaller 中间构建目录。 |
| `.pytest_cache/`、`.ruff_cache/` | 测试 / 代码检查缓存。 |

---

## 七、关键架构数据流（一句话串起来）

```
用户语音/文字
   → UIBridgeService（固定 main 会话 + 记忆注入 + 回复清理）
      → AgentService（ReAct 图：LLM 选工具 → 调 tools/*）
         → LLM（Qwen 对话）/ 视觉（GLM-4.1V）/ 浏览器（CDP）
      ← 最终回答（clean_response 过滤噪音）
   → UI 气泡展示 + TTS 扬声器播报
   ↺ 每轮后台提炼 → MemoryService → SQLite（长期记忆）
```

---

> **维护提示**：新增工具请在 `src/tools/` 建文件并在 `tools/__init__.py` 注册；新增服务在 `src/services/` 实现并由 `main.py`/`ui_bridge_service.py` 装配；所有路径取 `utils/path_utils.py`；所有密钥放 `.env`（勿硬编码）。