# 桌面语音助手 — 系统现状总结报告

> **生成时间**：2026-08-10
> **项目路径**：`D:\zhuomZNT`
> **运行环境**：Windows 11 + Python 3.12.7（`venv_assistant`）+ PySide6 6.6.3.1 + LangChain/LangGraph
> **当前阶段**：M0~M7 全部完成，M8 持续监听增强（进阶+高级版）、M9 网页自动化能力全部上线。视觉模型统一为智谱 GLM-4.1V。

---

## 一、总体评价

系统已从"可使用的个人助手"成长为**具备专业级网页自动化能力的智能桌面助手**：语音唤醒 → 实时转写预览 → 语义级智能断句 → LangGraph 智能体推理 → **27 个工具**（含 10 个 CDP 网页自动化工具）→ 语音播报，全链路闭环。

M8 实现了持续监听进阶+高级版：实时转写预览气泡（0.8s 增量刷新+光标动画）、收尾词提前结束（"执行/开始/去吧/就这样/好了"等强收尾词）、追加说话合并（1.0s 沉默窗口）、语义级智能断句（LLM 判断语义完整性），交互质感接近商用级语音助手。

M9 实现了完整网页自动化能力：通过 Chrome DevTools Protocol（CDP）WebSocket 长连接控制浏览器，10 个原子操作工具（导航/滚动/列元素/点击/输入/提取文本/标签管理），DOM 语义定位+GLM-4.1V 视觉模型辅助定位双引擎，域名白名单+高危操作拦截+密码框禁止输入等多级安全保护。

**一句话结论：功能丰富、稳定可靠、语音交互流畅，网页自动化可胜任搜索/阅读/填写等日常任务，已达到高频日常使用标准。**

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  UI 层（PySide6）                                            │
│  悬浮球(4状态) │ 抽屉面板 │ 调试面板 │ 设置中心 │ 历史 │ 托盘   │
│  + 实时转写预览气泡（M8）                                     │
├─────────────────────────────────────────────────────────────┤
│  桥接层 UIBridgeService（信号槽 + QThread 异步）               │
│  ├─ 任务打断确认状态机（代际守卫 + 12s 超时）                  │
│  ├─ 唤醒词监听管理（pause/resume 防回声自触发）                │
│  └─ 预览信号管理（增量ASR文本更新/最终化/清除）               │
├─────────────────────────────────────────────────────────────┤
│  智能体层 AssistantAgent（LangGraph ReAct 循环）              │
│  START → agent(LLM+27工具绑定) ⇄ tools(27工具) → END         │
│  + SQLite Checkpoint（跨会话记忆）+ 高危删除二次确认           │
├─────────────────────────────────────────────────────────────┤
│  服务层                                                      │
│  ASRService(SenseVoice离线) │ TTSService(Edge-TTS)           │
│  WakeWordService(常驻监听   │ SemanticSegmentService(M8)     │
│   + M8三态状态机)           │  (快速收尾词+LLM语义断句)       │
│  AppCatalogService(应用目录)│ BrowserAutomationService(M9)   │
│  SQLiteDB(设置/历史)        │  (CDP+DOM提取+截图+原子操作)    │
│  CheckpointService          │                                │
├─────────────────────────────────────────────────────────────┤
│  基础设施：Qwen Plus(文本LLM) │ GLM-4.1V-flash(视觉主力)      │
│            Qwen-VL(视觉备选)  │ 本地16kHz音频 │ websocket    │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心能力清单

### 3.1 语音链路（全部离线/免费，M8 增强）

| 能力 | 实现 | 实测表现 |
|------|------|---------|
| 语音唤醒 | 常驻麦克风 → 自适应基线 VAD（1s 窗口最低 20% 均值 × 系数）→ SenseVoice 检测"小助手" | 静音 CPU<0.5%，声学闭环验证通过 |
| **实时转写预览（M8 新）** | 唤醒后每 ~0.8s 做一次增量 ASR，UI 气泡实时更新文本+光标动画 ▍ | 用户说完之前即可看到自己说了什么，接近 Siri/小爱体验 |
| **收尾词提前结束（M8 新）** | 说话末尾检测到"执行/开始/去吧/就这样/好了/可以了/确定/吧/呗/啦"等强收尾词立即提交 | 不用等 1.2s 静音阈值，指令结束即响应 |
| **追加说话合并（M8 新）** | 尾点静音后进入 1.0s TAIL_WINDOW，重新说话则继续录音合并 | 用户停顿思考后补充内容不会被截断为两次指令 |
| **语义级智能断句（M8 新）** | 快速路径（关键词匹配）+ 慢速路径（LLM 判断语义完整性）双引擎 | 长指令（"先打开百度搜一下明天的天气然后顺便看看有没有适合的航班"）不会在中间停顿处误截断 |
| 语音识别（ASR） | FunASR + SenseVoiceSmall（本地离线，中/英/粤/日） | RTF≈0.09，2 秒语音 0.2 秒出结果 |
| 语音播报（TTS） | Edge-TTS 合成 + sounddevice 扬声器直播（不弹外部播放器） | 8 种音色别名，合成+播放全自动 |
| 免手指令 | "小助手，打开抖音"一句话直达；或先喊"小助手"→ 持续监听说指令 | 已验证 |
| 监听参数可调 | 尾点静音/最长录音/等待开口/能量系数/增量间隔/追加窗口 6 项进设置中心 | 保存即热生效 |

### 3.2 智能体工具集（**27 个**）

| 分类 | 工具 | 说明 |
|------|------|------|
| 📁 文件（4） | `create_file` / `search_files` / `recognize_file` / `delete_file` | 白名单路径保护；删除走 dry_run 预览 + 输入 DELETE 二次确认，优先进回收站 |
| 🌐 浏览器开关（3） | `open_browser` / `close_browser_tab` / `restore_browser_tab` | 100+ 站点快捷名；open_browser 对 Chrome/Edge 默认带 `--remote-debugging-port=9222` 启动；关标签 4 种粒度（站点批量/其他标签/重复站点/整个浏览器）；误关可语音恢复（Ctrl+Shift+T） |
| 🌐 **网页自动化（M9 新，10）** | `browser_navigate` / `browser_refresh` / `browser_go_back` / `browser_go_forward` / `browser_scroll` / `browser_list_elements` / `browser_click` / `browser_input` / `browser_extract_text` / `browser_list_tabs` | CDP WebSocket 长连接控制浏览器；DOM 可交互元素提取+序号定位；GLM-4.1V 视觉模型辅助图标/图片按钮定位；支持导航/翻页/点击/输入/回车提交/提取正文/标签切换；高危操作（支付/删除/密码/转账）自动拦截二次确认；密码框禁止自动填写 |
| 💻 桌面应用（1） | `open_app` | 识别本机 **144+ 个应用**（桌面+开始菜单扫描），模糊匹配启动，实测 34ms 打开 |
| 🖥️ 屏幕监控（2） | `list_active_apps` / `recognize_screen` | 轻量：1ms 枚举当前窗口+进程（本地）；视觉：截图 + **GLM-4.1V** 看懂屏幕内容（自动回退 Qwen-VL） |
| 🔊 系统（4） | `system_volume` / `system_screenshot` / `system_power` / `system_translate` | 音量/截图/锁屏待机/翻译（文本 LLM 翻译） |
| 📰 资讯（1） | `search_news` | 必应→百度→离线模拟三级降级 |
| 🎙️ 语音（2） | `voice_input` / `voice_output` | 供 Agent 链路内部调用 |

### 3.3 智能交互机制

- **任务打断确认**：执行 A 时来新指令 B → 气泡+语音询问"要中止当前任务执行新指令吗？" → 确认才替换（旧任务结果代际守卫丢弃、播报即时掐断）；否定/听不清/12 秒超时 → 保守保留当前任务。回答走免唤醒词持续监听（10s 窗口），也可打字。
- **高危操作确认**：① 文件删除必须两次调用 + 用户输入 DELETE；② 网页高危按钮（支付/付款/删除/密码/转账/验证码）自动拦截，要求用户明确确认；③ 密码框禁止自动填写。
- **跨会话记忆**：LangGraph SQLite Checkpoint，重启后同一话题可继续（"打开刚才那个文件"）。
- **回声自触发防护**：TTS 播报/手动按住说话期间暂停唤醒监听；Agent 思考期间保持监听（可语音插话）。
- **多级安全保护（M9）**：① 域名白名单（百度/知乎/B站/GitHub/淘宝等 40+ 常用站点）；② 高危操作关键词拦截；③ 敏感信息（密码/银行卡）不自动填写；④ 所有操作仅作用于助手启动的浏览器实例。

### 3.4 UI 形态（7 类全部落地 + M8 增强）

悬浮球（IDLE/LISTENING/THINKING/SPEAKING 四态 + 拖动 + 边缘吸附）、抽屉面板（含实时转写预览气泡）、彩色调试面板（7 阶段日志）、设置中心（含 M8 监听参数、M9 视觉模型配置）、历史 Tab、系统托盘、高危确认弹窗。

---

## 四、使用指南

### 启动方式

```
双击 D:\zhuomZNT\启动语音助手.bat
```

⚠️ 必须用此 bat（强制走虚拟环境 Python）。直接用系统 Python 会因缺 PySide6 秒退。

### 常用指令示例

| 你说 | 系统做 |
|------|--------|
| "小助手，打开抖音" | 浏览器打开抖音 |
| "小助手，关闭抖音" | 关闭对应标签页 |
| "小助手，打开微信" | 启动本机微信 |
| **"帮我在百度搜一下今天北京天气"** | **打开百度→自动找搜索框→输入→回车→读结果（M9）** |
| **"打开知乎看看热门话题"** | **CDP打开知乎→列出页面→读热门内容（M9）** |
| **"往下翻" / "回顶部"** | **网页滚动翻页（M9）** |
| **"帮我读一下这个页面"** | **提取网页正文并总结（M9）** |
| "我桌面上开了哪些应用" | 列出当前窗口 |
| "看看我屏幕上显示什么" | 截图 + GLM-4.1V 视觉识别 |
| "音量调到 50%" / "静音" | 系统音量控制 |
| "今天有什么热搜？总结 5 条" | 搜索 + 总结 + 播报 |
| "在桌面建个周报.md" | 创建文件 |
| "删掉 xxx 文件" | 弹窗确认后才执行 |
| "锁屏" | 锁定桌面 |

### 网页自动化标准流程（M9）

1. 说「打开XX网站」→ `open_browser` 以调试端口启动浏览器
2. 说「帮我搜XX / 点XX按钮」→ Agent 自动：`browser_list_elements` 列元素 → `browser_click/input` 按序号操作
3. 说「读一下页面」→ `browser_extract_text` 提取正文后总结
4. 高危操作（如"支付/删除"）→ 系统自动拦截 → 用户确认「确认点击X」后才执行

### 关闭/调整功能

- 关闭唤醒词监听：设置表 `wake_word_enabled=false` 重启
- 更换唤醒词：设置表 `wake_word`（默认"小助手"）
- 关闭 TTS 播报：面板开关或设置 `enable_tts=false`
- 监听参数调整：设置中心 → 语音页（尾点/最长录音/增量间隔/追加窗口等）

---

## 五、质量与稳定性

### 已修复的关键问题（全阶段累计）

| 问题 | 根因 | 状态 |
|------|------|:---:|
| "sounddevice 和 pyaudio 都未安装" | 报错时依赖确实未装 | ✅ |
| 文本指令报 `'str' object has no attribute 'get'` | `stream_events()` 回调式 API 被误当生成器遍历 | ✅ |
| TTS 弹外部播放器 | 播放优先级 system 在最前 | ✅ 改 sounddevice 直播 |
| ASR 识别成功却报"未解析到文本" | FunASR 返回 list[dict]，解析代码只处理 dict | ✅ |
| **启动几十秒后进程无声消失** | **PySide6 先于 torch 加载导致 c10.dll 初始化失败（段错误）** | ✅ torch 预加载移到 PySide6 import 之前 |
| bat 启动闪退 | 系统 Anaconda Python 缺 PySide6 | ✅ 专用启动器 |
| 预览气泡重复创建 | update_preview 逻辑冗余 | ✅ 简化为单一入口 |
| judge_speech_complete 参数错误 | semantic_segmenter 接口与调用不匹配 | ✅ |
| press_enter 只发 keyDown 无 keyUp | CDP 按键事件缺弹起 | ✅ 统一为 press_key(down+up) |
| page_go_back 残留无效代码 | 开发过程中的废弃代码 | ✅ 清理 |

### 测试资产（`tests/` 共 **21 个脚本**，单元测试累计 **200+ 用例**）

- `test_M0_skeleton.py` ~ `test_M6_package.py`：M0~M6 里程碑验收
- `test_M15_tools.py`：M1.5 文件工具增强
- `test_task_switch.py`：打断确认状态机（分类器 14 用例 + 状态机 5 场景）
- `test_app_catalog.py` / `test_app_tools.py`：应用目录 + 开 app + 监控 + 视觉识别全链路
- `test_browser_close_enhanced.py`：浏览器关闭增强（批量关闭 mock + 模式路由 + 降级 + 真机冒烟）
- `test_continuous_listen.py`：持续监听基础版 8 用例
- **`test_M8_advanced_listen.py`**（新）：M8 进阶+高级版持续监听（尾点提交/增量预览/收尾词/追加合并/语义断句等）
- **`test_M9_web_automation.py`**（新）：网页自动化 24 用例（DOM格式化/文本匹配/服务单例/安全机制/工具实例化/CDP降级处理）
- 完整 App 冒烟：长跑 5 分钟+ 稳定无崩溃

---

## 六、已知限制（诚实清单）

1. **关闭非激活标签页**：open_browser 默认带调试端口启动 Chrome/Edge，助手打开的浏览器可精确管理任意标签（含后台）。仅当用户手动提前打开无调试端口的浏览器时，新标签会被并入旧实例，工具会明确提示"彻底关闭浏览器后让助手重新打开"。
2. **能量 VAD 在嘈杂环境**（放音乐/视频）会误切段去识别（不会误唤醒，除非声音里真有"小助手"），阈值系数可调高。
3. **打包版 exe 无语音功能**：`Assistant.spec` 排除了 torch/funasr（约 2GB），exe 里 ASR 降级为纯文本模式。当前按源码运行为主。
4. **视觉模型隐私**：`recognize_screen` 和网页视觉定位会把截图发到智谱/通义千问云端，涉及敏感内容时留意；轻量窗口监控完全本地。
5. **任务"中止"是逻辑丢弃**：LangGraph 线程无法物理强杀，旧任务后台跑完但结果被代际守卫丢弃，不影响新任务。
6. **NumPy 警告**：PySide6 6.6.3.1 按 NumPy 1.x ABI 编译，在 numpy 2.4.6 下启动打印兼容警告，不影响运行（升级 PySide6 到 6.7+ 可消除）。
7. **网页自动化首次使用**：如果浏览器之前已经打开（无调试端口），需要先完全关闭浏览器后再说"打开XX网站"，之后即可获得完整自动化能力。
8. **语义断句 LLM 延迟**：M8 语义级断句的慢速路径（LLM 判断）在网络差时可能增加 0.5~1s 延迟，但快速路径（关键词）覆盖 80%+ 场景不受影响。

---

## 七、后续可选方向

| 方向 | 价值 | 成本 | 状态 |
|------|------|------|:---:|
| ~~浏览器默认带调试端口启动~~ | — | — | ✅ 已完成（2026-08-08） |
| ~~持续监听进阶版（实时预览+收尾词+追加合并）~~ | — | — | ✅ 已完成 M8（2026-08-10） |
| ~~持续监听高级版（语义级智能断句）~~ | — | — | ✅ 已完成 M8（2026-08-10） |
| ~~网页自动化基础版（10 个 CDP 工具+视觉定位+安全保护）~~ | — | — | ✅ 已完成 M9（2026-08-10） |
| ~~视觉模型统一为智谱 GLM-4.1V~~ | — | — | ✅ 已完成（2026-08-10） |
| ~~多 Agent 路由~~ | 单图 ReAct+27 工具已全覆盖 | — | ❌ 不实施（2026-08-08 定论） |
| 网页自动化进阶版（表单填写/多步任务/下拉选择/iframe） | 完成更复杂的网页任务 | 中（2~3天） | 📋 待规划 |
| 网页自动化高级版（自主浏览 Agent+多步推理） | "帮我订一张明天去上海的机票" | 大（1~2周） | 📋 待规划 |
| 全局热键注册（Ctrl+Space 按住说话） | 任意前台应用可语音输入 | 小 | 📋 |
| 打包版 exe 含语音（删 spec excludes 重打包） | 双击即用全功能 | exe+2GB，打包慢 | 📋 |
| 升级 PySide6 到 6.7+ 消除 NumPy 警告 | 日志干净 | 需回归测试 UI | 📋 |
| 唤醒词改专用 KWS 模型 | 更低 CPU、更低误触发 | 需找/训中文唤醒模型 | 📋 |
| 语音音色克隆（CosyVoice） | 个性化音色 | 大（需GPU+模型） | 📋 |

---

## 八、关键文件索引

```
启动语音助手.bat                    ← 唯一推荐启动入口
src/main.py                         ← 入口（含 torch 预加载，勿动顺序）
src/services/
  ├─ agent_service.py               ← LangGraph ReAct + 系统提示词（含 M8/M9 说明）
  ├─ ui_bridge_service.py           ← UI 桥接 + 任务打断状态机 + 预览信号
  ├─ wake_word_service.py           ← 唤醒 + 持续监听三态状态机（M8 重写）
  ├─ semantic_segment_service.py    ← M8 语义断句服务（快速关键词+慢速LLM）
  ├─ browser_automation_service.py  ← M9 CDP 浏览器自动化核心服务（新）
  ├─ asr_service.py / tts_service.py← 语音双引擎
  ├─ app_catalog_service.py         ← 本机应用目录（144+ 个）
  ├─ checkpoint_service.py          ← SQLite 会话记忆
  └─ sqlite_db.py                   ← 设置/历史持久化
src/tools/                          ← 27 个工具（8 个文件）
  ├─ __init__.py                    ← 工具统一注册入口
  ├─ web_automation_tools.py        ← M9 网页自动化 10 工具（新）
  ├─ browser_tools.py               ← 浏览器开关 3 工具
  ├─ file_tools.py                  ← 文件 4 工具
  ├─ app_tools.py                   ← 桌面应用+屏幕监控（recognize_screen 已改 GLM-4.1V）
  ├─ system_tools.py                ← 系统 4 工具（音量/截图/电源/翻译）
  ├─ news_tools.py                  ← 新闻搜索
  └─ voice_tools.py                 ← 语音输入输出
src/infra/
  └─ llm_client.py                  ← Qwen LLM 客户端
tests/                              ← 21 个验收脚本（M0~M9）
data/                               ← 运行期数据（SQLite/缓存/录音/截图临时文件）
.env                                ← 配置（QWEN_API_KEY / GLM_API_KEY 等）
```

---

*报告由 WorkBuddy 基于实际代码与运行验证生成，数据均为实测值。*
*最后更新：2026-08-10，覆盖 M0~M9 全部里程碑。*
