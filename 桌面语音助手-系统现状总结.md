# 桌面语音助手 — 系统现状总结报告

> **生成时间**：2026-08-08
> **项目路径**：`D:\zhuomZNT`
> **运行环境**：Windows 11 + Python 3.12.7（`venv_assistant`）+ PySide6 6.6.3.1 + LangChain/LangGraph
> **当前阶段**：M0~M6 全部里程碑已完成，并额外完成 3 轮功能增强迭代

---

## 一、总体评价

系统已从"脚手架"成长为**功能完整、可日常使用的桌面语音助手**：语音唤醒 → 离线识别 → LangGraph 智能体推理 → 17 个工具执行 → 语音播报，全链路闭环。两天内修复了 4 个关键 bug（含一个 Windows 下 PySide6/torch DLL 加载顺序导致的段错误），新增了唤醒词免手触发、任务打断确认、桌面应用识别、屏幕视觉监控四项重要能力。2026-08-08 又完成浏览器关闭能力增强迭代：open_browser 默认带调试端口启动、批量关闭 4 种粒度、关闭结果可感知、误关可恢复。

**一句话结论：能用、稳定、覆盖面广，已达到个人日常使用标准。**

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  UI 层（PySide6）                                            │
│  悬浮球(4状态) │ 抽屉面板 │ 调试面板 │ 设置中心 │ 历史 │ 托盘   │
├─────────────────────────────────────────────────────────────┤
│  桥接层 UIBridgeService（信号槽 + QThread 异步）               │
│  ├─ 任务打断确认状态机（代际守卫 + 12s 超时）                  │
│  └─ 唤醒词监听管理（pause/resume 防回声自触发）                │
├─────────────────────────────────────────────────────────────┤
│  智能体层 AssistantAgent（LangGraph ReAct 循环）              │
│  START → agent(LLM+工具绑定) ⇄ tools(16工具) → END           │
│  + SQLite Checkpoint（跨会话记忆）+ 高危删除二次确认           │
├─────────────────────────────────────────────────────────────┤
│  服务层                                                      │
│  ASRService(SenseVoice离线) │ TTSService(Edge-TTS)           │
│  WakeWordService(常驻监听)  │ AppCatalogService(应用目录)      │
│  SQLiteDB(设置/历史)        │ CheckpointService               │
├─────────────────────────────────────────────────────────────┤
│  基础设施：Qwen Plus(LLM) │ qwen-vl-max(视觉) │ 本地16kHz音频 │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心能力清单

### 3.1 语音链路（全部离线/免费）

| 能力 | 实现 | 实测表现 |
|------|------|---------|
| 语音唤醒 | 常驻麦克风 → 自适应能量 VAD 切段 → SenseVoice 检测"小助手" | 静音 CPU<0.5%，声学闭环验证通过 |
| 语音识别（ASR） | FunASR + SenseVoiceSmall（本地离线，中/英/粤/日） | RTF≈0.09，2 秒语音 0.2 秒出结果，测试 12/12 字符全中 |
| 语音播报（TTS） | Edge-TTS 合成 + sounddevice **扬声器直播**（不弹外部播放器） | 8 种音色别名，合成+播放全自动 |
| 免手指令 | "小助手，打开抖音"一句话直达；或先喊"小助手"→ 8 秒内说指令 | 已验证 |

### 3.2 智能体工具集（17 个）

| 分类 | 工具 | 说明 |
|------|------|------|
| 📁 文件 | `create_file` / `search_files` / `recognize_file` / `delete_file` | 白名单路径保护；删除走 dry_run 预览 + 输入 DELETE 二次确认，优先进回收站 |
| 🌐 浏览器 | `open_browser` / `close_browser_tab` / `restore_browser_tab` | 100+ 站点快捷名；open_browser 对 Chrome/Edge 默认带 `--remote-debugging-port=9222` 启动；关标签 4 种粒度（站点批量/其他标签/重复站点/整个浏览器），关闭后报告剩余标签数；误关可语音恢复（Ctrl+Shift+T） |
| 💻 桌面应用 | `open_app` | 识别本机 **144 个应用**（桌面+开始菜单扫描，解析率 98%），模糊匹配启动，实测 34ms 打开 |
| 🖥️ 屏幕监控 | `list_active_apps` / `recognize_screen` | 轻量：1ms 枚举当前窗口+进程（本地）；视觉：截图 + qwen-vl-max 看懂屏幕内容（1.6s） |
| 🔊 系统 | `system_volume` / `system_screenshot` / `system_power` / `system_translate` | 音量/截图/锁屏待机/翻译 |
| 📰 资讯 | `search_news` | 必应→百度→离线模拟三级降级 |
| 🎙️ 语音 | `voice_input` / `voice_output` | 供 Agent 链路内部调用 |

### 3.3 智能交互机制

- **任务打断确认**：执行 A 时来新指令 B → 气泡+语音询问"要中止当前任务执行新指令吗？" → 确认才替换（旧任务结果代际守卫丢弃、播报即时掐断）；否定/听不清/12 秒超时 → 保守保留当前任务。回答免唤醒词（10 秒等答案窗口），也可打字。
- **高危操作确认**：删除文件必须两次调用 + 用户输入 DELETE，无静默执行路径。
- **跨会话记忆**：LangGraph SQLite Checkpoint，重启后同一话题可继续（"打开刚才那个文件"）。
- **回声自触发防护**：TTS 播报/手动按住说话期间自动暂停唤醒监听，Agent 思考期间保持监听（可语音插话）。

### 3.4 UI 形态（7 类全部落地）

悬浮球（IDLE/LISTENING/THINKING/SPEAKING 四态 + 拖动 + 边缘吸附）、抽屉面板、彩色调试面板（7 阶段日志）、设置中心、历史 Tab、系统托盘、高危确认弹窗。

---

## 四、使用指南

### 启动方式

```
双击 D:\zhuomZNT\启动语音助手.bat
```

⚠️ 必须用此 bat（强制走虚拟环境 Python）。直接用系统 Python 会因缺 PySide6 秒退——这是之前"启动就终止"的根因。

### 常用指令示例

| 你说 | 系统做 |
|------|--------|
| "小助手，打开抖音" | 浏览器打开抖音 |
| "小助手，关闭抖音" | 关闭对应标签页 |
| "小助手，打开微信" | 启动本机微信 |
| "我桌面上开了哪些应用" | 列出当前窗口 |
| "看看我屏幕上显示什么" | 截图 + 视觉识别描述 |
| "音量调到 50%" / "静音" | 系统音量控制 |
| "今天有什么热搜？总结 5 条" | 搜索 + 总结 + 播报 |
| "在桌面建个周报.md" | 创建文件 |
| "删掉 xxx 文件" | 弹窗确认后才执行 |
| "锁屏" | 锁定桌面 |

### 关闭/调整功能

- 关闭唤醒词监听：设置表 `wake_word_enabled=false` 重启
- 更换唤醒词：设置表 `wake_word`（默认"小助手"）
- 关闭 TTS 播报：面板开关或设置 `enable_tts=false`

---

## 五、质量与稳定性

### 已修复的关键问题（本阶段）

| 问题 | 根因 | 状态 |
|------|------|------|
| "sounddevice 和 pyaudio 都未安装" | 报错时依赖确实未装（时间线还原确认） | ✅ |
| 文本指令报 `'str' object has no attribute 'get'` | `stream_events()` 回调式 API 被误当生成器遍历 | ✅ |
| TTS 弹外部播放器 | 播放优先级 system 在最前 | ✅ 改 sounddevice 直播 |
| ASR 识别成功却报"未解析到文本" | FunASR 返回 list[dict]，解析代码只处理 dict | ✅ |
| **启动几十秒后进程无声消失** | **PySide6 先于 torch 加载导致 c10.dll 初始化失败（段错误）** | ✅ torch 预加载移到 PySide6 import 之前 |
| bat 启动闪退 | 系统 Anaconda Python 缺 PySide6 | ✅ 专用启动器 |

### 测试资产（`tests/` 共 18 个脚本）

- `test_task_switch.py`：打断确认状态机（分类器 14 用例 + 状态机 5 场景）
- `test_app_catalog.py` / `test_app_tools.py`：应用目录 + 开 app + 监控 + 视觉识别全链路
- `test_browser_close_enhanced.py`：浏览器关闭增强（批量关闭 mock + 模式路由 + 降级 + 真机冒烟，RUN_LIVE=1 开启真机）
- M1~M6 各里程碑验收脚本
- 完整 App 冒烟：长跑 5 分钟+ 稳定无崩溃

---

## 六、已知限制（诚实清单）

1. **关闭非激活标签页**：已通过「open_browser 默认带调试端口启动 Chrome/Edge」根治——助手打开的浏览器实例可精确管理任意标签（含后台）。仅当用户手动提前打开的浏览器（无调试端口）在运行时，新标签会被并入旧实例导致端口不生效，工具会明确提示"彻底关闭浏览器后让助手重新打开一次"；此时降级为窗口匹配 + 循环 Ctrl+W 连续关闭。
2. **能量 VAD 在嘈杂环境**（放音乐/视频）会误切段去识别（不会误唤醒，除非声音里真有"小助手"），阈值参数 `THRESHOLD_RATIO` 可调。
3. **打包版 exe 无语音功能**：`Assistant.spec` 为瘦身排除了 torch/funasr（约 2GB），exe 里 ASR 降级为纯文本模式。当前按你的决策以源码运行为主。
4. **视觉识别隐私**：`recognize_screen` 会把屏幕截图发到通义千问云端，涉及敏感内容时留意；轻量窗口监控完全本地。
5. **任务"中止"是逻辑丢弃**：LangGraph 线程无法物理强杀，旧任务会后台跑完但结果被代际守卫丢弃，不影响新任务。
6. **NumPy 警告**：PySide6 6.6.3.1 按 NumPy 1.x ABI 编译，在 numpy 2.4.6 下启动打印兼容警告，不影响运行。

---

## 七、后续可选方向

| 方向 | 价值 | 成本 |
|------|------|------|
| 打包版 exe 含语音（删 spec excludes 重打包） | 双击即用全功能 | exe 体积 +2GB，打包慢 |
| 升级 PySide6 到 6.7+ 消除 NumPy 警告 | 日志干净 | 需回归测试 UI |
| 唤醒词改专用 KWS 模型（替代 VAD+ASR 轮询） | 更低 CPU、更低误触发 | 需训练/找中文唤醒模型 |
| 全局热键注册（Ctrl+Space 按住说话） | 任意前台应用可语音输入 | 小 |
| ~~浏览器默认带调试端口启动~~ | ✅ 已完成（2026-08-08，含批量关闭 4 粒度 + 恢复工具） | — |
| ~~多 Agent 路由（M6.3 可选项）~~ | ❌ 决策不实施：单图 ReAct + 17 工具已覆盖全部场景，supervisor 多一层 LLM 调用只增加延迟与成本，按计划"如无提升就不切"原则放弃（2026-08-08 定论） | — |

---

## 八、关键文件索引

```
启动语音助手.bat                    ← 唯一推荐启动入口
src/main.py                         ← 入口（含 torch 预加载，勿动顺序）
src/services/
  ├─ agent_service.py               ← LangGraph ReAct + 系统提示词
  ├─ ui_bridge_service.py           ← UI 桥接 + 任务打断状态机
  ├─ wake_word_service.py           ← 唤醒词常驻监听
  ├─ asr_service.py / tts_service.py← 语音双引擎
  └─ app_catalog_service.py         ← 本机应用目录（144 个）
src/tools/                          ← 17 个工具（7 个文件）
tests/                              ← 18 个验收脚本
data/                               ← 运行期数据（SQLite/缓存/录音，约 900MB）
```

---

*报告由 WorkBuddy 基于实际代码与运行验证生成，数据均为实测值。*
