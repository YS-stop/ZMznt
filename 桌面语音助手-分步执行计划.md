# 桌面语音助手系统 — 分步执行计划（Implementation Playbook）

> **关联文档**: `桌面语音助手-需求规格文档.md` v1.2  
> **版本**: v1.0  
> **适用环境**: Windows 10/11 + PowerShell 7+ + Python 3.11.x  
> **执行原则**（对齐 agentskills-main 工程标准 + Experience 1246097 教训）：
> - ⚠ **原则 1（环境单一）**：全程只用一个 Python 3.11 虚拟环境，不换版本、不并行启动多个后台；锁死 LangChain=1.3.12 / LangGraph=0.2.x / PySide6=6.6.x 大版本，小版本只升不降
> - ⚠ **原则 2（命令原生）**：Windows PowerShell 只用原生命令，不用类 Unix `head`/`curl -L`/`sed`/`awk`；下载用 `Invoke-WebRequest`（别名 `iwr`）
> - ⚠ **原则 3（路径动态）**：代码中所有路径用 `Path(__file__).parent / "xxx"` 动态拼接，**禁止硬编码本机用户名/绝对路径**
> - ⚠ **原则 4（子进程解释器对齐）**：ASR/OCR 若启动子进程，命令必须用 `sys.executable`（等于当前虚拟环境 python.exe），避免"主进程在 venv、子进程跑系统 Python"隐性不一致
> - ⚠ **原则 5（失败收敛）**：同类失败 ≥2 次，立即收敛策略，不循环"清缓存→重试→换版本→再清"；只清理当前错误直接相关的缓存子目录，不全局清 pip/wheel 缓存
> - ⚠ **原则 6（自动兜底）**：所有工作目录（文件操作默认目录、OCR临时目录、下载缓存）初始化时 `Path(...).mkdir(parents=True, exist_ok=True)`，不要运行到中途才发现路径不存在报错

---

## 文档结构速览

| 章节 | 内容 | 预估耗时 |
|------|------|----------|
| [M0. 环境与项目脚手架](#m0) | 虚拟环境创建、依赖安装、目录结构、.env、.gitignore、启动脚本 | 30~60 分钟 |
| [M1. 原型验证：文字版 Agent 跑通](#m1) | LangGraph 建 ReAct 图 + 3 个 LangChain Tool + Qwen + 极简 PySide6 窗口 | 1~2 周 |
| [M2. 语音接入 + 桌面 UI 雏形](#m2) | ASR/TTS 接通 + P-01 悬浮球 + P-02 抽屉雏形 + SQLiteCheckpointSaver | 1 周 |
| [M3. 文件工具集 + 高危确认条件边](#m3) | 文件 CRUD/全文搜索/OCR/摘要 + P-07 高危弹窗 + LangGraph 条件边 | 2 周 |
| [M4. 网络工具 + 资讯聚合](#m4) | 浏览器打开 + 新闻搜索 + 热搜爬取 + 工具并行执行 | 1 周 |
| [M5. UI 全面打磨](#m5) | P-03 调试面板（彩色日志）+ P-04 设置中心 + P-05 历史 + P-06 托盘 + Accordion 动画 | 1 周 |
| [M6. 打包 + 扩展工具](#m6) | PyInstaller 单 exe + 扩展工具集 + 可选多 Agent 路由 | 1 周 |
| [质量门禁与验收](#qa) | 每个里程碑的必过检查项 | 每次里程碑 15 分钟 |
| [回滚与版本策略](#rollback) | Git tag、虚拟环境快照、失败回退 | 每次里程碑 5 分钟 |
| [Windows 坑位速查表](#gotchas) | 常见报错与对应解决方案 | 随时查阅 |

---

<a name="m0"></a>
## M0. 环境与项目脚手架（一次性前置，30~60 分钟）

> **目标**：把项目从零到可 `python main.py` 启动的最小骨架建好，所有后续里程碑在此骨架上迭代。

### M0.1 Python 版本 & 虚拟环境（必须先做）

| 步骤 | 执行命令（Windows PowerShell 当前目录 `d:\zhuomZNT`） | 产出 | 验收标准 |
|------|-----------------------------------------------------|------|----------|
| M0.1.1 确认 Python 3.11 | `python --version` → 必须返回 `Python 3.11.x`；若没有 → 官网装 Python 3.11.9（勾选 Add to PATH，安装 for all users 勾选 Install for all users） | Python 3.11 | `python -c "import sys; print(sys.version)"` 以 3.11 开头 |
| M0.1.2 创建项目虚拟环境 `venv_assistant` | `python -m venv venv_assistant` | 目录 `d:\zhuomZNT\venv_assistant\` | `venv_assistant\Scripts\python.exe --version` 返回 3.11.x |
| M0.1.3 **激活虚拟环境（所有后续命令必须先激活！）** | `.\venv_assistant\Scripts\Activate.ps1`；若报 ExecutionPolicy → 先跑 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force`（只做一次） | PowerShell 前缀出现 `(venv_assistant) PS d:\zhuomZNT>` | `Get-Command python | Select-Object -ExpandProperty Source` 输出路径包含 `venv_assistant\Scripts\python.exe` |
| M0.1.4 升级 pip + 锁下载源（国内加速） | `python -m pip install --upgrade pip setuptools wheel`；然后新建 `venv_assistant\pip.ini`（内容见下方） | pip.ini | `pip config list` 能看到 index-url = 清华源 |

**`venv_assistant\pip.ini` 内容（粘贴进去即可，避免国外源超时）**：
```ini
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
extra-index-url = https://mirrors.aliyun.com/pypi/simple
trusted-host = pypi.tuna.tsinghua.edu.cn, mirrors.aliyun.com
timeout = 120
```

### M0.2 依赖安装（严格按顺序，不并行）

| 步骤 | 命令 | 预估耗时 | 验收标准 |
|------|------|----------|----------|
| M0.2.1 安装 PySide6（Qt 桌面 UI，最大 600MB，先装） | `pip install PySide6==6.6.3.1` | 3~10 分钟 | `python -c "import PySide6; from PySide6.QtWidgets import QApplication; print('PySide6 OK', PySide6.__version__)"` 无报错 |
| M0.2.2 安装 LangChain + LangGraph 双框架（指定大版本） | `pip install "langchain>=1.3.12,<1.4" "langgraph>=0.2.0,<0.3" "langchain-openai>=0.1.0,<0.2" "langchain-core>=0.3.0,<0.4" pydantic` | 2~5 分钟 | `python -c "import langchain, langgraph, langchain_openai; print('LC', langchain.__version__, 'LG', langgraph.__version__)"` 符合区间 |
| M0.2.3 安装工具依赖（文件全文索引/HTTP/语法高亮） | `pip install whoosh jieba python-docx PyPDF2 openpyxl requests beautifulsoup4 pygments` | 1~3 分钟 | 每个 import 均成功 |
| M0.2.4 安装 TTS（Edge-TTS，免费无需 Key） | `pip install edge-tts` | 30 秒 | `edge-tts --list-voices | Select-Object -First 3` 能列出中文音色 |
| M0.2.5 **ASR/OCR 留到 M2/M3 再装**（体积大，先不占资源）：DashScope SDK → M2；PaddleOCR → M3 | — | — | — |
| M0.2.6 导出初始依赖锁 | `pip freeze > requirements.lock.M0.txt` | 5 秒 | 文件 `requirements.lock.M0.txt` 存在，作为基线 |

### M0.3 项目目录结构（严格对齐 agentskills-main 分层）

执行以下命令创建目录（PowerShell）：
```powershell
# 全部用 mkdir -Force（等价 exist_ok=True）
$dirs = @(
    'src',
    'src\core',           # LangGraph 图、LangGraph 节点、状态
    'src\tools',          # LangChain Tool 类（文件/浏览器/资讯/OCR...）
    'src\ui',             # PySide6 控件（P-01~P-07 每个文件一个类）
    'src\services',       # 对话管理器、配置服务、SQLite 服务
    'src\infra',          # ASR 引擎、TTS 引擎、LLM 接口封装（与框架解耦）
    'src\utils',          # 路径工具、跨线程信号、通用异常
    'data',               # 运行期数据（自动创建，不要手改）
    'data\checkpoints',   # LangGraph SQLite Checkpoint
    'data\history',       # 对话历史 SQLite db
    'data\whoosh_index',  # Whoosh 全文索引目录
    'data\ocr_temp',      # PaddleOCR 临时图片
    'assets',             # SVG 图标、QSS 样式表
    'tests'               # 里程碑验收脚本（最小化）
)
foreach ($d in $dirs) { New-Item -ItemType Directory -Force -Path $d | Out-Null }
# 空 __init__.py 补齐包结构
$pkgDirs = @('src','src\core','src\tools','src\ui','src\services','src\infra','src\utils','tests')
foreach ($p in $pkgDirs) { if (-not (Test-Path "$p\__init__.py")) { New-Item -ItemType File -Force -Path "$p\__init__.py" | Out-Null } }
```

**最终目录树**（后续里程碑文件逐个追加）：
```
d:\zhuomZNT\
├── 📁 venv_assistant\           (虚拟环境，gitignore)
├── 📁 assets\                   (图标 SVG、QSS 样式)
├── 📁 data\                     (运行期数据，gitignore)
│   ├── app.db                   (SQLite: 配置+历史)
│   ├── checkpoints\             (LangGraph Checkpoint SQLite)
│   ├── whoosh_index\            (文件全文索引)
│   └── ocr_temp\                (OCR 临时图)
├── 📁 src\
│   ├── __init__.py
│   ├── main.py                  (启动入口，永远最小)
│   ├── 📁 core\                 (LangGraph 层)
│   │   ├── state.py             (TypedDict GraphState 定义)
│   │   ├── nodes.py             (agent_node / tools_node / confirm_node 函数)
│   │   ├── edges.py             (should_continue / is_high_risk 条件函数)
│   │   └── graph.py             (StateGraph 构建 + compile + SQLiteCheckpointSaver)
│   ├── 📁 tools\                (LangChain BaseTool 子类)
│   │   ├── file_tools.py        (create/search/delete/list/rename)
│   │   ├── browser_tools.py     (open_url/open_app)
│   │   ├── news_tools.py        (hot_search/web_search/summarize)
│   │   ├── ocr_tools.py         (image_ocr/doc_summary M3追加)
│   │   └── system_tools.py      (volume/screenshot M6追加)
│   ├── 📁 ui\                   (PySide6 QWidget 子类)
│   │   ├── floating_ball.py     (P-01 FloatingBallWidget)
│   │   ├── drawer_panel.py      (P-02 DrawerWidget + 5区块)
│   │   ├── debug_panel.py       (P-03 DebugPanel + Accordion 动画)
│   │   ├── settings_window.py   (P-04 SettingsMainWindow)
│   │   ├── history_tab.py       (P-05 HistoryTab)
│   │   ├── confirm_dialog.py    (P-07 HighRiskConfirmDialog)
│   │   └── widgets\             (ChipButton/AccordionSection/BubbleDelegate 公共子控件)
│   ├── 📁 services\             (业务服务，桥梁层)
│   │   ├── conversation_svc.py  (对话管理器：UI↔LangGraph 信号中转)
│   │   ├── settings_svc.py      (读写 SQLite settings 表)
│   │   └── sqlite_db.py         (统一 SQLite 连接 + 建表脚本)
│   ├── 📁 infra\                (外部依赖封装)
│   │   ├── llm_client.py        (LangChain ChatOpenAI 封装，QWEN_API_KEY 注入)
│   │   ├── asr_engine.py        (ASR 抽象类 + DashScopeASR + 未来 FunASR)
│   │   └── tts_engine.py        (TTS 抽象类 + EdgeTTSEngine + 未来 CosyVoice)
│   └── 📁 utils\
│       ├── path_utils.py        (project_root/data_root 动态 Path 函数，核心！禁止硬编码路径)
│       ├── qt_threading.py      (run_on_ui_thread 装饰器 + Worker QThread 基类)
│       └── exceptions.py        (AssistantBaseException + 各分类异常)
├── 📁 tests\
│   ├── test_M1_agent.py         (M1 后跑：文字指令→工具调用→结果)
│   ├── test_M2_voice.py         (M2 后跑：ASR 识别短句)
│   └── ... 每个里程碑一个验收脚本
├── .env                         (环境变量，gitignore)
├── .gitignore
├── requirements.lock.M0~M6.txt  (每阶段结束导出依赖快照)
└── 📄 桌面语音助手-需求规格文档.md / 分步执行计划.md
```

### M0.4 配置文件 & .gitignore & 启动骨架

#### M0.4.1 `.env`（项目根目录创建，填入你自己的 Key，后面所有地方从这里读）
```env
# ===== LLM（通义千问 OpenAI 兼容模式，必填）=====
QWEN_API_KEY=sk-ws-H.ERLIRRP.j9LR.MEQCIG1r5L6LVCvhoVUBQaCsJL7PXqh1qxIfF6qYd6aSaQp-AiAsHztmqtL9e9NbURlIEg41o28ERW9uuxE2TLaLsHlqxw
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-plus

# ===== ASR（阿里云 DashScope，与上面 Key 可以相同，M2 前填）=====
DASHSCOPE_API_KEY=sk-ws-H.ERLIRRP.j9LR.MEQCIG1r5L6LVCvhoVUBQaCsJL7PXqh1qxIfF6qYd6aSaQp-AiAsHztmqtL9e9NbURlIEg41o28ERW9uuxE2TLaLsHlqxw

# ===== TTS（Edge-TTS 不需要 Key，留空即可；CosyVoice 才填）=====
TTS_ENGINE=edge-tts
TTS_VOICE=zh-CN-XiaoxiaoNeural

# ===== 应用行为（M5 后可在 P-04 设置中心改，此处为默认值）=====
APP_WORKDIR=D:\zhuomZNT\data
APP_HOTKEY_SHOW=Ctrl+Alt+D
APP_HOTKEY_RECORD=Ctrl+Alt+V
APP_WAKE_WORD=小助手
APP_ENABLE_TTS=true
APP_DEBUG_PANEL_DEFAULT_OPEN=true
```

#### M0.4.2 `.gitignore`（项目根目录）
```gitignore
# 虚拟环境 & 缓存
venv_assistant/
__pycache__/
*.pyc
*.pyo

# 运行期数据（全部手改或自动生成，不入库）
data/
*.db
*.sqlite
*.sqlite3
*.log

# PyInstaller 打包产物
build/
dist/
*.spec

# 环境变量 & 锁文件
.env
requirements.lock.*.txt.bak

# IDE
.vscode/
.idea/
*.swp
```

#### M0.4.3 最小启动骨架 `src\main.py`（能跑起来显示空窗口，M1 开始填内容）
```python
"""桌面语音助手启动入口 —— 永远保持精简，所有业务逻辑放子包"""
import sys
from pathlib import Path
from dotenv import load_dotenv  # M0 先装 python-dotenv: pip install python-dotenv

# ★ 必须先加项目根到 sys.path，再 import src 内任何模块
PROJECT_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(PROJECT_ROOT))

from src.utils.path_utils import ensure_data_dirs  # M0 写这个函数
from src.services.sqlite_db import init_sqlite      # M0 写这个函数

def main():
    load_dotenv(PROJECT_ROOT / ".env")          # 加载 .env
    ensure_data_dirs()                           # 自动创建 data 下所有子目录（exist_ok）
    init_sqlite()                                # 自动建 settings/history/checkpoints 三张表

    # M1 前这里先只显示一个极简 QWidget，验证 PySide6
    from PySide6.QtWidgets import QApplication, QLabel
    app = QApplication(sys.argv)
    label = QLabel("桌面语音助手骨架 — M0 脚手架完成 ✔", alignment=5)
    label.resize(420, 80)
    label.show()
    sys.exit(app.exec())

if __name__ == "__main__":
    main()
```

#### M0.4.4 `src\utils\path_utils.py`（核心路径工具，所有代码从此拿路径，不要自己拼）
```python
"""统一路径工具：所有路径从这里取，禁止硬编码绝对路径"""
from __future__ import annotations
import os
from pathlib import Path
from typing import Final

PROJECT_ROOT: Final[Path] = Path(__file__).resolve().parents[2]   # d:\zhuomZNT
DATA_ROOT: Final[Path] = Path(os.getenv("APP_WORKDIR", PROJECT_ROOT / "data")).resolve()

# 子目录定义
CHECKPOINTS_DIR = DATA_ROOT / "checkpoints"
WHOOSH_INDEX_DIR = DATA_ROOT / "whoosh_index"
OCR_TEMP_DIR = DATA_ROOT / "ocr_temp"
ASSETS_DIR = PROJECT_ROOT / "assets"


def ensure_data_dirs() -> None:
    """启动时一次性创建所有运行期目录（幂等，重复调用无副作用）"""
    for d in (DATA_ROOT, CHECKPOINTS_DIR, WHOOSH_INDEX_DIR, OCR_TEMP_DIR, ASSETS_DIR):
        d.mkdir(parents=True, exist_ok=True)
```

### M0 验收标准（做完才可进入 M1）
1. ✅ 激活 venv 后，`python src\main.py` 弹出"桌面语音助手骨架"窗口，无报错
2. ✅ `tests\` 目录存在，`src\` 下 7 个子模块都有 `__init__.py`
3. ✅ `.env` 至少填了 QWEN_API_KEY，`python -c "import os; from dotenv import load_dotenv; load_dotenv(); print(bool(os.getenv('QWEN_API_KEY')))"` 返回 True
4. ✅ 所有数据目录存在：`Test-Path data\checkpoints, data\whoosh_index, data\ocr_temp` 三个都返回 True
5. ✅ `pip freeze > requirements.lock.M0.txt` 已生成备份

---

<a name="m1"></a>
## M1. 原型验证：文字版 Agent 跑通（1~2 周）

> **目标（只验证核心链路，不做 UI 美化/不接语音）**：文字输入 → LangGraph StateGraph 跑 ReAct 循环 → 自动选择 LangChain Tool 执行 → 返回结果。  
> **交付物**：`tests\test_M1_agent.py` 3 个验收用例全部通过。

### M1.1 LLM 客户端封装

| 步骤 | 产出文件 | 关键实现要点 | 验收 |
|------|----------|------------|------|
| M1.1.1 | `src\infra\llm_client.py` → 函数 `get_qwen_llm()` 返回 `ChatOpenAI(...)` | `base_url = os.getenv("QWEN_BASE_URL")`, `api_key=os.getenv("QWEN_API_KEY")`, `model=os.getenv("QWEN_MODEL","qwen-plus")`, `temperature=0` | `python -c "from src.infra.llm_client import get_qwen_llm; llm=get_qwen_llm(); print(llm.invoke('你好，只回复OK').content)"` 输出包含 OK |
| M1.1.2 | `src\core\state.py` → `class AgentState(TypedDict)` | 字段必含：`messages: Annotated[list[BaseMessage], add_messages]`、`pending_confirm: dict \| None`、`thread_id: str` | import 无报错 |

### M1.2 LangChain 封装 3 个 Tool（MVP 最小集）

每个 Tool 继承 `langchain_core.tools.BaseTool`，严格实现 `name` / `description`（给 LLM 看的描述要详细！） / `args_schema: Pydantic Model` / `_run()` 方法。

| 步骤 | 产出文件 | 功能 | 验收测试命令 |
|------|----------|------|------------|
| M1.2.1 | `src\tools\file_tools.py` → `CreateFileTool` | 入参 `path: str` + `content: str`，用 Path 写；路径不在白名单内提示"路径不在允许范围" | 工具 `.invoke({"path":"D:/zhuomZNT/data/m1_test.txt","content":"hello"})` 成功创建文件 |
| M1.2.2 | `src\tools\file_tools.py` → `SearchFilesTool` | 入参 `query: str` + `search_path: str = 桌面`，用 glob + Path 遍历；返回前 5 条文件路径字符串 | 先建个文件再 invoke，能找到自己创建的 |
| M1.2.3 | `src\tools\browser_tools.py` → `OpenBrowserTool` | 入参 `url_or_name: str`，内置 `{"github":"https://github.com","百度":"https://www.baidu.com","邮箱":"https://mail.qq.com"}` 补全；用 `import webbrowser; webbrowser.open()` | invoke("百度") → 默认浏览器打开百度（本地会真开，验收脚本里可以用 mock） |
| M1.2.4 | `src\tools\__init__.py` → `get_all_tools()` 返回 Tool 列表 + `TOOL_MAP` 字典 | 统一注册入口；后续加工具只改这里 | `len(get_all_tools()) == 3` |

### M1.3 LangGraph 建图（核心！）

| 步骤 | 产出文件 | 实现要点 |
|------|----------|----------|
| M1.3.1 节点函数 | `src\core\nodes.py` → `def agent_node(state: AgentState) -> dict` 与 `def tools_node(state: AgentState) -> dict` | `agent_node` 内部：把 tools bind 到 LLM `llm_with_tools = get_qwen_llm().bind_tools(tools)` → `return {"messages": [llm_with_tools.invoke(state["messages"])]}`；`tools_node`：`ToolNode(tools)` 执行 |
| M1.3.2 条件边函数 | `src\core\edges.py` → `def should_continue(state) -> Literal["tools","__end__"]` | 判断 `messages[-1].tool_calls` 是否非空 → 空就 `__end__`，否则 `"tools"` |
| M1.3.3 构建+编译图 | `src\core\graph.py` → `def build_agent_graph(checkpointer=None) -> CompiledGraph` | `StateGraph(AgentState)` → `.add_node("agent", agent_node)` → `.add_node("tools", tools_node)` → `.set_entry_point("agent")` → `.add_conditional_edges("agent", should_continue)` → `.add_edge("tools", "agent")` → `graph.compile(checkpointer=checkpointer)`；**M1 先用 MemorySaver()，M2 换 SQLite** |
| M1.3.4 工具注册表 | 把 `get_all_tools()` 通过闭包或全局注入到 nodes.py 里的 agent_node | 不直接 import 避免循环依赖 |

### M1.4 对话管理器雏形 + 测试脚本

| 步骤 | 产出 | 关键实现 |
|------|------|----------|
| M1.4.1 | `src\services\conversation_svc.py` → 类 `ConversationService` | 方法 `async def run_text(self, text: str, thread_id: str) -> str`：内部 `graph.ainvoke({"messages": [HumanMessage(content=text)]}, config={"configurable": {"thread_id": thread_id}})` → 返回 `res["messages"][-1].content` |
| M1.4.2 | `tests\test_M1_agent.py` | 3 个用例（用 `unittest` 或 `pytest`，二选一装 pytest 轻量）：<br>① `test_create_file`：输入"在 data 目录创建一个 m1_create.txt，内容是 M1 通过" → 断言文件存在且内容对<br>② `test_search_file`：输入"查找 data 目录里后缀是 .txt 的文件" → 返回列表包含刚才创建的 m1_create.txt<br>③ `test_open_browser_mock`：用 `unittest.mock.patch("webbrowser.open")` 断言输入"打开 GitHub"会调用 webbrowser.open("https://github.com") |

### M1 验收标准
```powershell
# 在项目根执行，激活 venv 后
pip install pytest
pytest tests\test_M1_agent.py -v
# 结果：3 passed in x seconds
```

---

<a name="m2"></a>
## M2. 语音接入 + 桌面 UI 雏形（1 周）

> **目标**：ASR（语音→文字）+ TTS（文字→语音）打通；P-01 悬浮球、P-02 抽屉雏形能点能用；LangGraph Checkpoint 持久化到 SQLite。

### M2.1 TTS 先接（免费 Edge-TTS，不依赖 Key 以外的任何服务）

| 步骤 | 产出 | 验收 |
|------|------|------|
| M2.1.1 | `src\infra\tts_engine.py` → 基类 `BaseTTSEngine` + `EdgeTTSEngine` 实现 | `tts.speak("语音助手启动成功", blocking=True)` → 电脑扬声器有声音；结果保存到 `data\tts_cache\` 相同文本不重复下载（省流量） |

### M2.2 ASR 接通（DashScope 实时 API）

| 步骤 | 产出 | 关键要点 | 验收 |
|------|------|----------|------|
| M2.2.1 | `pip install dashscope==1.20.*` + `src\infra\asr_engine.py` → `DashScopeASR` | 录音用 `sounddevice` / `pyaudio`：**子进程启动 microphone 录音时，子进程命令第一个参数写死 `sys.executable` 不是 "python"**（对齐原则 4） | 短句"打开百度"识别为文字，字符合集 90% 以上匹配 |
| M2.2.2 | 抽象 `BaseASREngine` 定义 `start() / stop() / add_callback(cb)` 接口 | M4 换 FunASR 只新增一个子类不破坏上层 | import 正常 |

### M2.3 LangGraph Checkpoint 持久化

| 步骤 | 产出 | 验收 |
|------|------|------|
| M2.3.1 | `src\core\sqlite_checkpoint.py`（或 pip install langgraph-checkpoint-sqlitesaver 现成包） | 关掉程序再重开，对同一个 thread_id 说"打开第二个"，Agent 能基于上一轮上下文执行成功（断点恢复） |

### M2.4 PySide6 两大核心 UI

| 步骤 | 产出文件 | 实现要点（对齐架构文档） | 验收 |
|------|----------|------------------------|------|
| M2.4.1 | `src\ui\floating_ball.py` → 类 `FloatingBallWidget(QWidget)` | 4 种状态枚举（S0~S3）+ 自绘圆形 + 拖动 + 边缘吸附；**状态切换用自定义信号 `stateChanged(int)`** 通知抽屉 | 启动后右下角显示 56px 蓝色圆形；能拖到屏幕任意位置；单击发送 `toggleDrawer()` 信号 |
| M2.4.2 | `src\ui\drawer_panel.py` → 类 `DrawerWidget(QWidget)` | 无边框 + `QPropertyAnimation` 右侧滑入；先做 5 区块骨架（顶栏/空占位/空占位/快捷 Chip/输入框），Accordion 动画留到 M5 | 点悬浮球 → 抽屉从右侧滑出出现；输入框 Enter 发送文字信号 |
| M2.4.3 | 接入对话管理器 | Drawer 发送文字 → `ConversationService` 异步调 LangGraph → 收到回答在占位气泡区追加 + TTS 播报（开/关读取设置） | 语音按住说话 → ASR 出字 → AI 回复气泡显示 + 发声 |

### M2 验收：`python src\main.py` 实机走一遍
1. 右下角悬浮球显示
2. 按 `Ctrl+Alt+V` 开始录音，说"在 data 目录新建一个 m2_test.txt，写测试"
3. 松开 → ASR 识别→AI 回答"已创建"→ 文件 `data\m2_test.txt` 存在
4. 关程序再打开，说"打开刚才那个文件" → 正常打开（证明 Checkpoint 生效）

---

<a name="m3"></a>
## M3. 文件工具集 + 高危确认条件边（2 周）

| 步骤 | 产出 | 关键点 |
|------|------|--------|
| M3.1 文件工具补全 | `file_tools.py` 追加：`DeleteFileTool`（分 recycle / permanent）、`RenameFileTool`、`WhooshSearchContentTool`、`ListDirTool` | **删除类 Tool 的 `_run` 不直接删**，只返回待删清单 → 真正删除由条件边弹窗确认后再执行 |
| M3.2 OCR + 摘要 Tool | `pip install paddleocr paddlepaddle cpu 版本` → `ocr_tools.py`：`ImageOCRTool`、`DocxSummarizeTool`（LangChain 分块 + LLM refine 摘要） | PaddleOCR 模型目录路径同样用 Path 动态拼接；大 PDF 分块 2000 tokens 一次 |
| M3.3 LangGraph 高危确认条件边 | `src\core\edges.py` 新增 `is_high_risk_tool_call(state) -> bool`；`nodes.py` 新增 `confirm_node`；`graph.py` 在 agent→tools 之间插入条件边 | 删除/覆盖类 → 挂起 → 发 Qt 信号到主线程弹 P-07 Dialog → 用户结果写回 state → 继续 / 中止 |
| M3.4 P-07 高危弹窗 | `src\ui\confirm_dialog.py` → `HighRiskConfirmDialog(QDialog)` | DELETE 输入框匹配成功才 enable 确认按钮；Esc = 取消；永久删除必须走 DELETE；纯回收站删除走 5s 倒计时 |
| M3.5 验收脚本 | `tests\test_M3_fileops.py` | ①创建→②搜索→③尝试删除走弹窗→④用户输入DELETE→⑤确认删除成功 |

---

<a name="m4"></a>
## M4. 网络工具 + 资讯聚合（1 周）

| 步骤 | 产出 | 关键点 |
|------|------|--------|
| M4.1 浏览器工具扩展 | `browser_tools.py` 追加：`OpenAppTool`（调用 `os.startfile()` / subprocess 打开本机 exe，路径从 settings 取） | 子进程启动命令写 `subprocess.Popen([app_path], executable=sys.executable if py else None)` |
| M4.2 资讯工具集 | `news_tools.py`：`HotSearchTool`（爬微博/知乎热搜公开 API）、`WebSearchTool`（requests + BeautifulSoup 爬 DuckDuckGo HTML 或用 SearXNG）、`NewsSummarizeTool`（LLM 总结成简报） | 全部请求加 `timeout=10s`，异常包装友好提示"网络未连接"不崩溃 |
| M4.3 工具并行执行（可选） | `langgraph.graph.ToolNode` 构造时 `parallel=True` + 多个 `tool_calls` 并行 | 对"同时打开 A 和 B 网站"类型指令性能提升 |
| M4.4 验收 | 指令：①"打开 Photoshop" → 真打开 / 弹应用未安装提示；②"今天有什么热搜？总结 5 条" → 返回简报 + TTS 播报 |

---

<a name="m5"></a>
## M5. UI 全面打磨（1 周）

| 步骤 | 产出 | 关键点 |
|------|------|--------|
| M5.1 公共控件库 | `src\ui\widgets\`：`AccordionSection`（标题箭头 + 高度动画 `QParallelAnimationGroup`）、`ChipButton`（圆角 Chip）、`BubbleListWidget`（QListWidget + 自定义 BubbleDelegate 左/右气泡）、`SwitchToggle`（自绘 iOS 风开关） | 统一一处，所有窗体复用 |
| M5.2 P-03 调试面板 | `src\ui\debug_panel.py` | `QPlainTextEdit` + `QSyntaxHighlighter` 给 ASR/Agent/Tool/Observation 上色；订阅 `ConversationService` 的 debug_log 信号 → `QMetaObject.invokeMethod` 投递 |
| M5.3 P-04 设置中心 | `src\ui\settings_window.py` | 左侧 5 个 AccordionSection（通用/语音/快捷键/AI密钥/隐私）；右侧 `QStackedWidget` + `QFormLayout`；快捷键捕获自定义 LineEdit；保存写 `settings_svc` |
| M5.4 P-05 历史 Tab | `src\ui\history_tab.py`；左侧日期分组 QListWidget + 摘要卡片 | 详情回放 Dialog 气泡 + 调试日志逐条高亮 |
| M5.5 P-06 托盘菜单 | main.py 里 `QSystemTrayIcon` 菜单 + 4 状态图标切换 | 关闭按钮不退出，托盘"退出程序"二次确认才 `qApp.quit()` |
| M5.6 验收 | 所有 7 类形态都能点出来；Accordion 上下箭头旋转动画流畅；调试面板 7 阶段彩色日志正确显示 |

---

<a name="m6"></a>
## M6. 打包 + 扩展工具（1 周）

| 步骤 | 产出 | 关键点（防坑！）|
|------|------|----------------|
| M6.1 PyInstaller spec 配置 | 根目录 `Assistant.spec` | ① `hidden-import` 加全：`langchain, langgraph, langchain_openai, paddleocr, paddle.base`（否则运行时报 ModuleNotFoundError）<br>② `datas` 加 `assets`、`data` 空目录结构、PaddleOCR 模型目录<br>③ `--noconsole` 正式版、`--console` 开发版 |
| M6.2 扩展工具按需加 | `system_tools.py`：音量（pycaw）、截图（`QScreen.grabWindow`）、锁屏/待机（`ctypes.windll.user32.LockWorkStation`）、翻译（Qwen 直接翻） | 每项单独 Tool，注册到 tools/__init__.py 就行，Graph 不需要重建 |
| M6.3 可选：多 Agent 路由升级 | `src\core\supervisor_graph.py` → 1 个 supervisor 节点 + FileAgent/NewsAgent/SystemAgent 3 个子图 | 对比单图性能与效果，如无提升就不切（保持简单） |
| M6.4 打包验收 | 拷贝 `dist\桌面语音助手.exe` 到一台**没装 Python 的 Windows 电脑**运行一遍所有 M1~M5 用例（必做！） | 能正常启动、调 Key、弹悬浮球、调工具、弹高危确认 |

---

<a name="qa"></a>
## 质量门禁（每个里程碑必须过，不打折扣）

每个里程碑结束前，按以下清单逐条打 ✔：

### QA-1 代码规范（agentskills 要求）
1. 所有 `src/**/*.py`：`ruff check src --fix` 或 `flake8 src` 无 E/W 级错误
2. 所有公共函数有 docstring + 入参/返回类型注解（PEP 484）；没有 `TODO: xxx` 遗留未实现
3. 路径全部走 `path_utils.py`，全文 grep `D:\\Users` / `C:\\Users` / `os.getcwd()` → 结果为 0 条（禁止硬编码本机路径）
4. 任何启动子进程的地方，grep `subprocess` / `Popen` → 命令行第一个参数是 `sys.executable` / 绝对路径 exe，不用裸 "python"

### QA-2 线程与 UI 安全（Qt 生死线！）
1. 全代码搜索 `show()/exec()/setText()/appendPlainText()` 等 UI 调用 → 必须发生在主线程；worker 线程调用都套了 `run_on_ui_thread` 装饰器或 `QMetaObject.invokeMethod(..., Qt.QueuedConnection)`
2. LangGraph 全程跑在 `QThread Worker` 里，不能阻塞 UI（app 不卡不白屏不 Not Responding）

### QA-3 异常与回滚（用户体验底线）
1. 没网 / Key 失效 / LLM 超时：UI 顶部 toast 友好提示，不崩；点击悬浮球 S3 红边
2. 文件删除 / 覆盖：永远走 P-07 Dialog，**没有任何静默执行路径**
3. 工具重试 2 次都失败：AI 自动回答"这个操作失败了，错误原因：xxx，建议怎么怎么办"，而不是抛异常栈给用户

### QA-4 资源占用（非功能需求落地）
1. 空闲（开着不动 2 分钟）任务管理器 CPU < 2%、内存 < 200MB
2. 录音 + 推理峰值 < 500MB（M1 可放宽，M6 必须压到）

### QA-5 依赖锁与 Git
1. 里程碑结束 → `pip freeze > requirements.lock.Mx.txt`（x=1~6）归档，**下一里程碑只在此基础上增量加，不降级**
2. Git tag：`git tag Mx-done -m "Milestone x passed"` + `git push --tags`（回滚用）

---

<a name="rollback"></a>
## 回滚与版本策略（Experience 教训：卡死时别硬撑，秒回上一阶段）

**任何时候遇到 ≥3 个无法短时间修复的环境/依赖故障 → 按此步骤回滚，不原地死循环重试**：

1. **Git 回滚代码**：`git reset --hard M{x-1}-done`（回到上一个已通过里程碑 tag）
2. **虚拟环境回滚**：删 `venv_assistant\` → 重新 `python -m venv venv_assistant` → 激活 → `pip install -r requirements.lock.M{x-1}.txt`（不要试图"修"当前 venv，直接重建最快）
3. **数据目录保留**：`data\` 整个复制到 `data_backup_before_Mx\` 作为兜底，不直接操作备份
4. **收敛执行顺序**：回滚后 **只一个窗口、一个命令跑**，不要并行开多个终端装依赖 / 跑测试 / 启动程序（容易出 lock 文件冲突，对应 Experience Gradle lock 教训）

---

<a name="gotchas"></a>
## Windows 环境坑位速查表（必看！少走弯路）

| # | 现象 / 报错 | 根因（90% 情况） | 一步解决命令 / 方案 |
|---|-------------|----------------|---------------------|
| 1 | PowerShell `.\venv\Scripts\Activate.ps1` 报 `无法加载，因为在此系统上禁止运行脚本` | ExecutionPolicy 限制 | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force`（用户级，不影响别人） |
| 2 | PySide6 启动报 `This application failed to start because no Qt platform plugin could be initialized` | 缺少平台 DLL 或 PATH 里有其他 Qt 冲突 | 激活 venv 后加一条：`$env:QT_QPA_PLATFORM_PLUGIN_PATH = "$PWD\venv_assistant\Lib\site-packages\PySide6\plugins\platforms"` |
| 3 | pip install 超时 / 404 / 断流 | 国外源网络不稳 | 已在 M0.1.4 写了 pip.ini 走清华源；若还不稳 → 每个包单独 `pip install xxx --timeout 300`，不要一次性装一堆 |
| 4 | LangGraph `graph.ainvoke` 调不到 Tool / 报 `No module named 'xxx_tool'` | tools 未正确 bind 给 LLM 或循环 import | 所有 Tool 统一从 `src.tools.get_all_tools()` 拿，不要各自 import；在 `graph.py` 最后 bind 到 llm_with_tools |
| 5 | 子进程 OCR/ASR "找不到模块 paddleocr"（原则 4 坑） | 子进程用了"python"指向系统 Python，而不是 venv 的 python.exe | 所有 Popen 第一个参数写死 `sys.executable`，例如 `Popen([sys.executable, "src/infra/_ocr_worker.py", img_path])` |
| 6 | Qt `ASSERT failure in QWidget: "Widgets must be created in the GUI thread."` 崩溃 | LangGraph worker 线程直接 `new QDialog / show / setText` | 封装 `@run_on_ui_thread` 装饰器（内部 `QMetaObject.invokeMethod`）；所有 UI 更新套一层装饰器 |
| 7 | PyInstaller 打包后 exe 运行秒退 / 空白 / ModuleNotFound | hidden-import 没加全 / datas 没加 / 控制台模式看不到报错 | 先用 `pyinstaller --console --onefile main.py` 打包 console 版，双击运行看黑窗报错，把缺的模块逐个加到 `hidden-import` 列表再切 `--noconsole` |
| 8 | pip 长时间停在 `Preparing metadata (pyproject.toml)` 不动 | 卡在编译 C 扩展（PaddleOCR / NumPy） | Ctrl+C 取消 → `pip install --prefer-binary xxx` 强制用预编译 wheel，不本地编译 |
| 9 | LangGraph Checkpoint 存 SQLite 报 `database is locked`（Experience lock 教训） | 多线程同时写一个 SQLite 连接 | SQLite 全局 singleton 一个连接；所有写操作包 `BEGIN IMMEDIATE` 事务 + 统一 `QSemaphore(1)` 单线程锁 |
| 10 | `Path(__file__).parent` 在 PyInstaller 打包后指向错误路径（临时解压 _MEIPASS） | 打包后 __file__ 语义变化 | `path_utils.py` 加分支：`if getattr(sys, "frozen", False): PROJECT_ROOT = Path(sys.executable).parent.parent else: 原来的计算` |

---

> **计划执行顺序建议**：M0 必做 → M1 做完跑通 pytest → 你确认文字链路 OK 之后再进 M2~M6。  
> **每个里程碑结束后的动作**：先跑对应 `tests/test_Mx_*.py` 全通过 → 打 QA 门禁 → 打 Git tag → 锁依赖 `requirements.lock.Mx.txt` → 再进入下一阶段。  
> 任何阶段卡住不要硬扛，优先用回滚策略，然后带着具体报错信息再定位原因。
