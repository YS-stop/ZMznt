# 桌面语音助手系统 — 需求规格文档 (PRD)

> **版本**: v1.2 (明确桌面原生 UI + LangChain + LangGraph 双架构)  
> **日期**: 2026-08-07  
> **目标平台**: Windows 10/11（纯桌面原生应用，**非 Web / 非浏览器 / 非 Electron**）  
> **核心定位**: 基于 **LangChain（工具与LLM封装）+ LangGraph（状态图编排与记忆）** 双框架构建的大模型 Agent 桌面语音助手，通过语音/文字指令执行文件操作、系统控制、资讯检索等任务。

---

## ⚠ 重要设计澄清（v1.2 新增，避免误解）

1. **不是 Web 端 / 浏览器应用**：所有界面（悬浮球、抽屉、设置、弹窗、托盘菜单）均采用 **PySide6（Qt for Python）Windows 桌面原生控件** 实现，是标准 `.exe` 桌面程序；不使用 HTML/CSS/JS、不跑浏览器内核、不依赖 Web 技术栈。
2. **Agent 不是只用 LangChain**：采用 **LangChain + LangGraph 双框架配合**：
   - **LangChain 职责**：LLM（Qwen）接口封装、Prompt/Message 模板、Tool 定义与注册、文档分块/OCR 封装等"原子能力层"；
   - **LangGraph 职责**：ReAct 循环状态图构建、工具调用节点编排、Checkpoint（会话断点恢复）、高危操作条件边判断、后续多 Agent 路由切换等"流程编排与状态管理层"。

---

## 一、项目概述

### 1.1 产品愿景
打造一个"能听懂话、会办事"的桌面 AI 助手，用户只需通过自然语言（语音或文字）下达指令，系统自主理解意图、规划步骤、调用工具完成任务，全程低/零交互。

### 1.2 设计原则
- **自然交互优先**：语音为主，文字为辅，支持唤醒词和快捷键双触发
- **LangChain + LangGraph 双驱动**：LangChain 管能力封装，LangGraph 管流程与状态
- **可扩展架构**：功能以"LangChain Tool 类 + LangGraph 节点"形式接入，方便后续无限扩展
- **桌面原生、轻量无感**：PySide6 悬浮球 + 抽屉式 Qt 控件，不干扰正常桌面工作

---

## 二、功能需求清单

### 2.1 语音交互模块（核心入口）

| 编号 | 功能点 | 详细说明 | 优先级 |
|------|--------|----------|--------|
| V-01 | 唤醒词触发 | 预设唤醒词（如"小助手"），识别到后启动录音，等待指令 | P0 |
| V-02 | 按键触发 | 支持全局快捷键（如 `Ctrl+Alt+V`）按住说话 / 按下开始录音再按结束 | P0 |
| V-03 | 实时语音转文字 (ASR) | 录音流实时转文字，显示在对话区，支持中文普通话 | P0 |
| V-04 | 语音合成播报 (TTS) | 任务完成/询问确认时，用自然语音播报结果 | P1 |
| V-05 | 录音状态可视化 | 悬浮球/抽屉显示录音中波形动画，避免用户"不知道有没有在听" | P1 |
| V-06 | 多轮对话上下文 | 支持"接着刚才说""把那个改成 XX"这类依赖上下文的指令 | P1 |

### 2.2 文件操作工具集（你提到的重点）

#### 2.2.1 创建文件 (F-CREATE)
| 子功能 | 说明 | 示例指令 |
|--------|------|----------|
| 新建空白文件 | 在指定路径创建空的 txt/md/docx/xlsx 等 | *"在桌面新建一个叫会议记录的 Word 文档"* |
| 按模板创建 | 根据用户描述的内容生成文件并写入 | *"帮我写一份周报模板，保存到 D盘周报目录"* |
| 创建文件夹 | 支持多级目录创建 | *"在项目文件夹下新建 src、assets、docs 三个子文件夹"* |

#### 2.2.2 查找文件 (F-SEARCH)
| 子功能 | 说明 | 示例指令 |
|--------|------|----------|
| 按文件名搜索 | 支持模糊匹配、通配符，指定盘符/目录范围 | *"找一下上周的财务报表，在 D盘 里"* |
| 按内容搜索 | 文本类文件（txt/md/docx/pdf）全文关键词搜索 | *"找包含'项目验收'字样的所有文档"* |
| 按修改时间/类型过滤 | 时间范围 + 文件类型组合筛选 | *"找最近 3 天修改过的图片"* |
| 结果预览与打开 | 列表展示结果，支持直接打开/定位所在目录 | *"打开第二个文件所在的位置"* |

#### 2.2.3 删除文件 (F-DELETE)
| 子功能 | 说明 | 示例指令 |
|--------|------|----------|
| 回收站删除 | 默认移入回收站，可恢复 | *"把桌面那个叫 tmp.txt 的文件删掉"* |
| 永久删除 | 带强二次确认，避免误删 | *"彻底删除 D盘 缓存目录里的所有 log 文件"* |
| 批量删除 | 按条件批量删除（类型/时间/目录） | *"清空下载文件夹里超过半年没用的压缩包"* |
| **安全确认机制** | 删除前必须二次确认（语音或文字交互），高危操作列出清单让用户复核 | 强制要求 |

#### 2.2.4 识别文件 (F-RECOGNIZE)
| 子功能 | 说明 | 示例指令 |
|--------|------|----------|
| 图片内容识别 (OCR) | 识别截图/照片中的文字，输出文本或保存为文件 | *"识别这张截图里的文字，保存到 notes.txt"* |
| 文档内容摘要 | 长文档（pdf/docx/txt）自动摘要核心内容 | *"帮我快速看一下这份合同的重点条款"* |
| 格式转换识别 | 识别文件真实格式（扩展名伪装检测）、编码识别 | *"这个文件打不开，识别一下真实格式"* |
| 元数据查看 | 图片 EXIF、文档作者、创建时间等信息提取 | *"看看这张照片是什么时候拍的"* |

### 2.3 网络与浏览器工具集

#### 2.3.1 打开浏览器 / 网页 (W-OPEN)
| 子功能 | 说明 | 示例指令 |
|--------|------|----------|
| 打开默认浏览器 | 启动系统默认浏览器 | *"打开浏览器"* |
| 打开指定网址 | 直接导航到 URL（支持模糊域名补全） | *"打开 GitHub"* / *"打开百度"* |
| 打开本地常用网站 | 内置常用站点快捷名（邮箱、云盘、工作后台等） | *"打开我的邮箱"* |

#### 2.3.2 搜索最新资讯 (W-NEWS)
| 子功能 | 说明 | 示例指令 |
|--------|------|----------|
| 通用关键词搜索 | 搜索引擎搜索关键词，返回摘要+链接 | *"搜索一下 AI 行业最近的新闻"* |
| 热点资讯聚合 | 拉取各大平台热搜榜单（微博/知乎/百度等） | *"今天有什么热搜？"* |
| 垂直领域资讯 | 科技/财经/体育等分类资讯 | *"最近股市有什么动态？"* |
| 资讯摘要总结 | 多条新闻汇总成简报，支持朗读 | *"把今天的科技新闻给我播报一下"* |

### 2.4 预留扩展能力（后续迭代）
> 用于后续"功能差不多全能"的扩展，架构上提前预留工具插槽：

| 类别 | 功能方向 | 示例 |
|------|----------|------|
| 系统控制 | 音量调节、亮度、待机、锁屏、截图 | *"把电脑静音"* |
| 应用启动 | 打开/关闭桌面应用、进程管理 | *"打开 Photoshop"* |
| 定时提醒 | 闹钟、倒计时、日程提醒 | *"30 分钟后提醒我开会"* |
| 快捷翻译 | 中英互译、多语种翻译 | *"把这句话翻译成英文：... "* |
| 代码助手 | 简单代码生成、命令解释、报错查询 | *"Python 怎么读 Excel？"* |
| 邮件/消息 | 读取邮件、发送消息（需授权） | *"读一下今天的未读邮件"* |

---

## 三、系统整体架构设计

### 3.1 架构总览（分层 + LangChain + LangGraph 双框架）

```
┌─────────────────────────────────────────────────────────────────────┐
│                  桌面原生 UI 层 (PySide6 / Qt)                       │
│   P-01悬浮球  P-02抽屉主面板  P-03调试面板  P-04设置  P-07确认弹窗  │
│   (全部为 Qt 原生 QWidget，非 Web/HTML)                               │
└────────────────────────┬────────────────────┬───────────────────────┘
                         │  输入事件 / 状态    │  UI 渲染指令
                         ▼                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    编排调度层 (Orchestrator)                          │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  LangGraph：状态图编排与记忆（流程与状态层）                   │  │
│  │  ┌─────────┐  ┌──────────┐  ┌────────────┐  ┌─────────────┐  │  │
│  │  │LLM节点  │→│工具调用节 │→│高危确认条件│→│Checkpoint存  │  │  │
│  │  │(ReAct推 │  │点(执行LC  │  │边(需不需  │  │储(SQLite版) │  │  │
│  │  │  理)    │  │  Tool)   │  │要弹窗?)    │  │(会话断点恢复│  │  │
│  │  └────┬────┘  └────┬─────┘  └────────────┘  └─────────────┘  │  │
│  │       │▲循环      │                                            │  │
│  │       └┘──条件边: │ Observation → 还需再推理? → 回到LLM节点    │  │
│  └────────────────────┼────────────────────────────────────────────┘  │
│                       │ LangChain 回调事件 ─→ 调试面板实时日志       │
│  ┌────────────────────▼────────────────────────────────────────────┐  │
│  │  LangChain：原子能力封装层（工具与LLM接口）                       │  │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐  │  │
│  │  │ Tool 注册表 │ │ Qwen Chat  │ │ Prompt模板 │ │ 文档/OCR封装│  │  │
│  │  │ (所有工具类)│ │ 模型接口   │ │ (ReAct/系统│ │ (分块/摘要)│  │  │
│  │  └────────────┘ └────────────┘ └────────────┘ └────────────┘  │  │
│  └────────────────────┬────────────────────────────────────────────┘  │
└───────────────────────┼───────────────────────────────────────────────┘
                        │ 工具函数调用
┌───────────────────────▼───────────────────────────────────────────────┐
│                     工具执行层 (Native Tool Layer)                     │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌─────┐ │
│  │ 文件操作Tool│ │ 识别Tool   │ │ 浏览器Tool │ │ 资讯Tool   │ │扩Tool│ │
│  │ CRUD+搜索  │ │ OCR+文档摘要│ │ 打开+导航  │ │搜索+热点聚合│ │扩展插槽│ │
│  └─────┬──────┘ └─────┬──────┘ └─────┬──────┘ └─────┬──────┘ └──┬──┘ │
└────────┼───────────────┼───────────────┼───────────────┼──────────────┘
         │               │               │               │
┌────────▼───────────────▼───────────────▼───────────────▼──────────────┐
│                      基础设施层 (Infra)                                │
│  ASR/TTS引擎  │  QWEN兼容接口  │  SQLite(配置+历史+Checkpoint) │ 日志  │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.2 核心模块职责说明（LangChain 与 LangGraph 严格分工）

| 模块 | 归属框架 | 职责 | 关键设计点 |
|------|----------|------|------------|
| **LangGraph StateGraph (编排层核心)** | LangGraph | 整个 Agent 的有向状态图：节点编排 + 条件边 + 循环控制 + 断点恢复 | 用 `StateGraph` 构建 ReAct 图；所有中间状态写入 `MemorySaver(SQLite)`；重启程序可恢复到上次中断的步骤 |
| **LLM 推理节点** | LangGraph 节点 | 调用大模型进行 ReAct 推理：输出 Thought / Action / Action Input / Final Answer | 内部使用 LangChain 的 `ChatPromptTemplate` + Qwen Chat 模型；输出强制解析为 Pydantic ToolCall 结构 |
| **工具调用节点** | LangGraph 节点 | 接收上一节点的 ToolCall，执行对应的 LangChain Tool 并返回 Observation | 支持并行工具调用（后续）；失败自动重试 2 次，错误包装为 Observation 返回给 LLM 自纠错 |
| **高危确认条件边** | LangGraph 条件边 | 工具执行前判断：是否属于删除/覆盖/批量操作 → 是则挂起图运行，弹出 P-07 桌面原生确认对话框，用户确认后才继续 | 图状态挂起期间存入 Checkpoint，用户关闭程序下次打开仍可回到"待确认"状态 |
| **Checkpoint 存储器** | LangGraph | 持久化图的每一步状态（用户消息、LLM 输出、工具结果、当前节点） | SQLite 版 `BaseCheckpointSaver`（参考 LangGraph 官方实现）；避免用 Redis/Mongo 等外部服务 |
| **对话管理器** | 服务层 (Qt) | UI ↔ LangGraph 的桥梁：拼接上下文、历史记录读取、向调试面板推送事件 | 订阅 LangGraph 的 `on_llm_start/on_tool_start/on_tool_end` 等回调，实时推送到 P-03 调试面板 |
| **Tool 注册表** | LangChain | 所有工具类（文件/OCR/浏览器/资讯）的统一注册处 | 每个工具继承 LangChain `BaseTool`，实现 `_run()`、`name`、`description`、`args_schema`；新工具 = 写类 + `register_tool()` 1 行 |
| **Qwen 模型封装** | LangChain | 大模型接口适配 | 用 `ChatOpenAI(base_url="...", api_key=QWEN_API_KEY, model="qwen-plus")` 兼容模式 |
| **ASR/TTS 引擎** | 基础设施 | 语音识别与合成 | 与 LangChain/LangGraph 解耦；结果通过 Qt 信号传入对话管理器 |
| **桌面原生 UI 层** | PySide6 (Qt) | P-01 ~ P-07 全部界面控件 | 全部是 `QWidget` / `QMainWindow` / `QDialog` 子类，纯桌面原生，无 Web 组件；Qt 信号槽机制与编排层通信 |

### 3.3 数据流（LangGraph 状态图节点流转，一次完整指令生命周期）

```
  [用户] 说话/打字
    │
    ▼
P-01 悬浮球 录音态动画  →  ASR 转文字
    │
    ▼  Qt 信号 (新消息)
对话管理器 ──▶ 组装 LangChain Message 列表 ──▶ 写入 GraphState["messages"]
    │
    ▼  启动 LangGraph StateGraph astream_events()
┌──────────────────────────────── 状态图节点循环 ────────────────────────────────┐
│                                                                               │
│   [节点 1: agent (LLM推理)]                                                   │
│     │  输入 GraphState["messages"]                                            │
│     │  Prompt = 系统提示(ReAct) + 工具描述 + 历史消息                          │
│     │  调用 Qwen 模型 → 得到 AIMessage（含 tool_calls 或 最终回答）            │
│     │  LangChain 回调 on_llm_end → 推调试面板 "Agent_Think"                  │
│     ▼                                                                         │
│   [条件边 1: 是否有 tool_calls？]                                              │
│     ├─ 否（是 Final Answer）─────▶ 跳出循环，到「输出节点」                    │
│     └─ 是（需要调工具）─────▶ 继续下一个判断                                  │
│                                                                               │
│   [条件边 2: Tool 类型是否高危？(删除/覆盖/批量) ]                             │
│     ├─ 是 ──▶ GraphState["pending_confirm"]=True                             │
│     │        ▼  Qt 信号触发                                                   │
│     │      P-07 高危确认弹窗（桌面原生 QDialog）                               │
│     │        等待用户输入 DELETE / 语音确认 / 取消                             │
│     │        ▼  确认结果写回 GraphState                                      │
│     │     取消 → 生成"用户已取消" Final Answer → 跳出循环                     │
│     │     确认 → 继续执行工具节点                                             │
│     └─ 否（低危操作，直接过） ──▶ 继续                                        │
│                                                                               │
│   [节点 2: tools (执行LangChain Tool)]                                        │
│     │  输入上一节点的 tool_calls                                               │
│     │  逐个从 Tool 注册表找到对应 Tool 实例并 .invoke()                       │
│     │  每个工具 on_tool_start/on_tool_end 回调 → 推调试面板                  │
│     │  返回 ToolMessage (Observation) 追加到 GraphState["messages"]          │
│     ▼                                                                         │
│   [无条件边: 工具执行完永远回到 agent 节点]                                   │
│     └──── 回到 [节点 1: agent]，让 LLM 基于 Observation 继续推理              │
│          （可以是再调一个工具，也可以输出最终答案）                            │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
    │
    ▼  跳出循环后，GraphState["messages"][-1] 即为最终 AI 回答
对话管理器
    ├─▶ 发送 Qt 信号 → P-02 对话记录区显示 AI 气泡
    ├─▶ 如果 TTS 开启 → 调用 TTS 引擎播报
    └─▶ 异步保存完整对话历史（LangGraph Checkpoint 已自动持久化）
```

---

## 四、技术选型建议

### 4.1 主技术栈（结合你熟悉的 Python 生态 + LangChain+LangGraph 双框架）

| 层面 | 选型方案 | 版本要求 | 推荐度 | 职责与理由 |
|------|----------|----------|--------|------------|
| **编程语言** | Python | 3.11+ | ★★★★★ | 你熟悉，Qt/ASR/LLM/文件操作生态最成熟；PySide6 与 LangGraph 对 3.11+ 支持最好 |
| **GUI 框架（桌面原生，非 Web）** | PySide6 (Qt for Python 6.6+) | ≥6.6 | ★★★★★ | **Windows 原生 QWidget 控件实现**（非 WebView/非 Electron/非 HTML），悬浮球无边框窗口、抽屉滑入动画、QTimer 驱动过渡动效全部原生支持 |
| **Agent 编排框架** | **LangGraph** | ≥0.2.0 | ★★★★★ | **流程与状态管理核心**：StateGraph 构建 ReAct 循环图、条件边判断高危操作、`SQLiteCheckpointSaver` 实现会话断点恢复、后续多 Agent 路由扩展 |
| **Agent 能力封装框架** | **LangChain** | ≥1.3.12 | ★★★★★ | **原子能力层**：`BaseTool` 工具基类、`ChatPromptTemplate` ReAct 模板、`ChatOpenAI` Qwen 兼容接口、RecursiveCharacterTextSplitter 文档分块；**不使用** LangChain 自带的旧版 AgentExecutor，全部由 LangGraph 接管执行流程 |
| **LLM 接口** | 通义千问 (Qwen) OpenAI 兼容模式 | qwen-plus / qwen-max | ★★★★★ | 你的偏好，`QWEN_API_KEY` 环境变量注入，原生支持 `tool_calls` 格式，适配 LangGraph 的 `bind_tools()` |
| **ASR 语音识别** | 方案 A: 阿里云 DashScope Paraformer 实时 API<br>方案 B: FunASR (阿里本地开源，CPU 推理) | — | ★★★★☆<br>★★★★☆ | A: 联网即用，中文识别率 98%+<br>B: 完全离线，隐私友好，单文件集成不依赖服务 |
| **TTS 语音合成** | 方案 A: 阿里云 CosyVoice API<br>方案 B: Edge-TTS (免费微软语音) | — | ★★★★☆<br>★★★★★ | B 免费、音色自然、无需 Key，MVP 优先落地；A 留作付费高质量切换项 |
| **文件全文搜索** | Whoosh 纯 Python 全文索引 | ≥2.7 | ★★★★☆ | 零外部依赖，索引文件存本地 SQLite 同级目录，支持中文分词（jieba 插件） |
| **OCR 文件识别** | PaddleOCR (PaddlePaddle CPU 版) | ≥2.7 | ★★★★★ | 中文手写/印刷体识别率极高，CPU 可跑，PyInstaller 可打包 |
| **本地数据存储** | SQLite (Python 内置 `sqlite3`) | — | ★★★★★ | 3 张表：对话历史 `chat_history`、LangGraph Checkpoint `checkpoints`、应用配置 `settings`，无需额外服务 |
| **打包发布** | PyInstaller + UPX 压缩 | ≥6.0 | ★★★★★ | 单 exe 或单目录输出，用户双击即用；提前处理好 PaddleOCR/LangChain 动态依赖收集 |

> **LangChain + LangGraph 分工口诀**：
> - 有"类/接口/封装"的需求 → 找 **LangChain**（写 Tool、拼 Prompt、调模型）
> - 有"流程/循环/判断/持久化"的需求 → 找 **LangGraph**（画 StateGraph、加条件边、存 Checkpoint）

### 4.2 方案组合建议（MVP → 完整版，全程 LangGraph 建图）

**阶段一（M1：原型验证，先把文字版 Agent 跑通）**
- GUI：PySide6 空壳窗口 + 纯文字输入框（不做悬浮球和动画，验证主链路）
- Agent 核心：**LangGraph 建 ReAct 状态图**（`agent` 节点 + `tools` 节点 + 循环条件边）+ LangChain 封装 3 个 Tool（create_file / search_files / open_browser） + Qwen 模型
- 验证目标：输入文字指令 → LangGraph 自动调工具 → 拿到结果，不做语音/不做 UI 美化

**阶段二（M2：语音接入 + 基础 UI）**
- GUI：P-01 悬浮球（4 种状态切换） + P-02 抽屉雏形（5 区块先不做 Accordion 动画）
- 语音：ASR 接入 DashScope、TTS 接入 Edge-TTS
- Agent：加入 `SQLiteCheckpointSaver`，验证重启程序会话可恢复

**阶段三（M3：完整文件工具集 + 高危确认）**
- 工具集：补齐文件 CRUD / Whoosh 全文索引 / PaddleOCR / 文档摘要
- LangGraph：新增**高危确认条件边**，删除类工具执行前必须挂起 → 弹 P-07 QDialog 确认 → 用户输入 DELETE 才继续节点
- UI：P-07 高危弹窗原生实现

**阶段四（M4：网络工具 + 资讯聚合）**
- 工具集：浏览器 `webbrowser` 打开、新闻热搜爬取、搜索引擎摘要
- LangGraph：可选引入多 Tool 并行执行（`ToolNode` `parallel=True`）

**阶段五（M5：UI 打磨 + 调试面板 + 设置）**
- UI：P-02 Accordion 折叠动画（QPropertyAnimation 实现高度变化）、P-03 调试面板终端风日志、P-04 设置分类、P-05 历史 Tab、P-06 托盘菜单
- 调试面板：订阅 LangGraph `astream_events()` 的事件流，彩色终端风显示 agent/tool/observation 每一步

**阶段六（M6：打包 + 扩展工具）**
- PyInstaller 单 exe 打包 + PaddleOCR 依赖处理验证
- 按需补 Tool：系统音量、应用启动、定时提醒、翻译、代码助手
- 可选：LangGraph 升级为多 Agent 路由（文件 Agent / 资讯 Agent / 系统 Agent 由 `supervisor` 节点切换）

---

## 五、桌面原生 UI/UX 窗体与面板体系规划（PySide6 Qt 控件，非 Web）

> **设计风格定位**：AI-Native UI + Soft UI Evolution + Voice-First Multimodal  
> **核心理念**：语音优先、轻量无感、信息分层清晰、抽屉折叠不占桌面、调试可视化透明。  
> **⚠ 实现方式强声明**：以下全部形态**均为 PySide6 Qt 原生 QWidget 子类**实现（无边框 QWidget、QMainWindow、QDialog、QSystemTrayIcon），**不使用任何 HTML / CSS / WebView / Electron / Web 技术**；颜色、字体、圆角、阴影用 Qt 样式表 QSS 或 QPainter 自绘实现；动效用 QPropertyAnimation + QTimer 驱动。

### 5.0 窗体与面板总览（共 7 类桌面原生形态）

| 编号 | 窗体/面板名称 | 形态类型（对应 Qt 控件类） | 触发方式 | 核心功能 | 优先级 |
|------|----------|----------|----------|----------|--------|
| P-01 | **桌面悬浮球** | `QWidget`（无边框 `Qt.FramelessWindowHint` + `Qt.WindowStaysOnTopHint`） | 程序启动后默认显示，可拖到桌面任意位置 | 录音状态可视化、点击展开/收起主面板、唤醒词提示 | P0 |
| P-02 | **抽屉式主对话面板** | `QWidget`（无边框，`QPropertyAnimation` 实现右侧滑入滑出） | 点击悬浮球 / 全局快捷键 `Ctrl+Alt+D` | 对话主界面（气泡聊天）、输入区、功能入口总枢纽 | P0 |
| P-03 | **消息同步调试面板** | Accordion 折叠区块（嵌在 P-02 内，`QScrollArea` + 自定义 `QWidget` + `QParallelAnimationGroup` 实现上下箭头高度动画） | P-02 顶栏"调试"按钮切换 | 可视化消息流转：ASR→Agent→Tool→Observation→Answer 全链路状态 | P0（开发期）P1（用户期） |
| P-04 | **设置中心窗口** | `QMainWindow`（带 Windows 原生标题栏，模态显示在 P-02 之上） | P-02 顶栏齿轮图标 / 托盘菜单"设置" | API Key、唤醒词、快捷键、TTS 开关、主题、权限等 | P0 |
| P-05 | **历史会话区** | P-02 内嵌 `QTabWidget` 切换页（`QListWidget` + 自定义 ItemDelegate） | P-02 顶栏"历史"Tab | 历史对话列表、按日期分组、单条回看、清空历史 | P1 |
| P-06 | **系统托盘菜单** | `QSystemTrayIcon` + `QMenu`（Windows 托盘右键原生菜单） | 单击/右键任务栏右下角托盘图标 | 快速开关（唤醒词/TTS）、显示面板、退出、设置 | P1 |
| P-07 | **高危操作确认弹窗** | `QDialog`（ApplicationModal 模态，居中显示在 P-02 之上） | 删除文件/永久删除等高危操作时由 LangGraph 条件边触发 | 列出操作清单、DELETE 文字二次确认输入、取消/确认按钮 | P0 |

> **窗体关系说明**：P-01 是常驻桌面入口，P-02 是核心容器（内嵌 P-03 Accordion、P-05 TabPage），P-04/P-07 是独立模态窗口、P-06 是托盘图标，全部围绕"语音指令执行"一条主线。

---

### 5.1 设计系统规范（Design System）

#### 5.1.1 配色方案（AI-Native 科技蓝 + 柔和中性色）

| 用途 | 色值（Light 模式） | 说明 |
|------|--------------------|------|
| Primary 主色 | `#3B82F6` (Blue-500) | 主按钮、录音中高亮、AI 气泡左侧条、品牌色 |
| Primary 深 | `#1D4ED8` (Blue-700) | 主按钮按下态、强调文字链接 |
| Primary 浅 | `#DBEAFE` (Blue-100) | AI 气泡背景、悬浮球录音态外圈脉冲 |
| 成功色 | `#10B981` (Emerald-500) | 工具执行成功、创建完成状态点 |
| 警示色 | `#F59E0B` (Amber-500) | Agent 思考中、澄清询问、加载状态 |
| 危险色 | `#EF4444` (Red-500) | 删除按钮、高危操作警告文字 |
| 用户气泡背景 | `#3B82F6` / 文字 `#FFFFFF` | 右侧用户消息，蓝色填充白色文字 |
| AI 气泡背景 | `#F8FAFC` (Slate-50) / 文字 `#0F172A` | 左侧 AI 消息，浅灰底深色字 |
| 调试面板背景 | `#0F172A` (Slate-900) / 文字 `#E2E8F0` | 深底浅字，终端感，区分普通对话 |
| 全局背景 | `#FFFFFF` | 主面板干净白底 |
| 边框分割线 | `#E2E8F0` (Slate-200) | 抽屉各区块分隔 |
| 悬浮球待机 | 渐变 `#60A5FA → #3B82F6` | 静态时蓝渐变圆形 |
| 悬浮球录音中 | 外圈 `#FCA5A5` 脉冲 + 内圈 `#EF4444` | 红色脉动，强提示"正在听" |

> **深色模式（Dark）**：后续迭代追加，P-04 设置中可切换，当前版本默认 Light。

#### 5.1.2 字体与间距

| 类别 | 规范 |
|------|------|
| 中文字体 | `微软雅黑 (Microsoft YaHei UI)` → 系统默认，无需额外打包 |
| 等宽字体（调试面板代码/参数） | `Consolas` / `Cascadia Mono` |
| 标题字号 | 18px / 24px / 28px（粗体 700） |
| 正文字号 | 14px（常规 400）；13px 次要文字；12px 标签/时间戳 |
| 气泡行高 | 1.6，长文本自动换行 |
| 基础间距单位 | 4px 为一档：`xs=4 / s=8 / m=12 / l=16 / xl=24 / xxl=32` |
| 圆角 | 控件圆角 `8px`，气泡圆角 `12px`，悬浮球完美圆形（`50%`），设置卡片 `12px` |
| 阴影 | Soft UI 轻阴影：`0 2px 8px rgba(15,23,42,0.06)`；主面板 `0 8px 32px rgba(15,23,42,0.12)` |

#### 5.1.3 动效与交互反馈（符合 Voice-First 特性）

| 交互场景 | 动效规范 | 时长 |
|----------|----------|------|
| 抽屉弹出/收起 | 右侧滑入滑出（translate X 400px → 0）+ 同步淡入 | 250ms，ease-out |
| 悬浮球点击 | 按下缩放 0.9 → 释放 1.0，配合轻微涟漪 | 150ms |
| 录音态脉冲 | 悬浮球外圈 `box-shadow` 呼吸扩散 1 → 1.6 倍，循环 | 1.6s loop |
| 气泡进入 | 新消息出现时从底部位移 12px + 淡入 | 200ms |
| Accordion 展开/收起 | 高度从 0 → auto 平滑过渡，箭头 `rotate 180°` | 220ms |
| Agent 思考中 | AI 气泡内显示 3 个点跳动动画（typing indicator） | 1.2s loop |
| 工具执行进度 | 调试面板中当前步骤左侧圆点 `green pulsing` | 步骤未完成时保持 |
| 高危确认弹窗 | 背景 mask 淡入 + 对话框从中心缩放 0.95→1.0 | 180ms |

> **无障碍**：所有带图标的按钮必须有文字 tooltip；可使用 `Tab` 键切换焦点；支持 `Esc` 关闭窗口/抽屉。

---

### 5.2 各窗体/面板详细布局 + PySide6 控件选型与实现要点

#### 5.2.1 P-01 桌面悬浮球（核心入口，P0）

**PySide6 实现要点**：
- 继承 `QWidget`，构造函数设置 `setWindowFlags(Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint | Qt.Tool)`（Tool 类型避免出现在任务栏）
- `setAttribute(Qt.WA_TranslucentBackground, True)` 使背景透明，然后重写 `paintEvent()` 用 `QPainter` + `QRadialGradient` 自绘圆形渐变、脉冲外圈、麦克风 SVG 图标
- 4 种状态切换用 `QStateMachine` + 3 个 `QPropertyAnimation`（录音态用 `QTimer` 16ms 刷新一次波纹半径）
- 拖动：重写 `mousePressEvent / mouseMoveEvent / mouseReleaseEvent`，`QPoint` 记录鼠标相对偏移，边缘吸附用 20px 阈值判断后 `move()` 到边缘

**尺寸与位置**：直径 56px 圆形，默认桌面右下角（距离下边缘 40px，右边缘 40px），支持鼠标左键按住拖到桌面任意位置，边缘自动吸附（吸附范围 20px）。

**状态与视觉**（共 4 种状态）：

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   [待机态 S0]        [唤醒/录音态 S1]      [思考中 S2]       │
│                                                             │
│   ┌──────────┐       ┌─────────────────┐     ┌──────────┐   │
│   │ ╭──────╮ │       │  ╭── OUTER ──╮  │     │ ╭──────╮ │   │
│   │ │  🎙  │ │       │  │ (脉冲扩散) │  │     │ │  ⠋   │ │   │
│   │ │ 图标 │ │       │  │  ╭────╮   │  │     │ │旋转中 │ │   │
│   │ ╰──────╯ │       │  │  │🔴听 │   │  │     │ ╰──────╯ │   │
│   │ 蓝渐变   │       │  │  │正在说│   │  │     │ 琥珀色   │   │
│   └──────────┘       │  │  ╰────╯   │  │     └──────────┘   │
│                      │  ╰───────────╯  │                    │
│                      └─────────────────┘                    │
│                                                             │
│   [错误/断网 S3]                                              │
│   ┌──────────┐                                                │
│   │ ╭──────╮ │   鼠标右键悬浮球 → 弹出快捷菜单：              │
│   │ │  ⚠   │ │   · 显示主面板                                  │
│   │ │图标红 │ │   · 唤醒词开关 ✔︎                               │
│   │ ╰──────╯ │   · TTS 播报开关 ✔︎                             │
│   │   红边   │   · 暂停 / 退出程序                              │
│   └──────────┘                                                │
└─────────────────────────────────────────────────────────────┘
```

| 状态 | 触发条件 | 视觉表现 | 点击/交互 |
|------|----------|----------|-----------|
| S0 待机 | 空闲无操作 | 蓝色渐变填充 + 白色麦克风 SVG 图标，轻阴影 | 单击 → 切换 P-02 抽屉显示/隐藏；右键 → 托盘快捷菜单 |
| S1 录音中 | 唤醒词命中 / 按下快捷键开始录音 | 内圈红色实心 + 外圈半透明红呼吸脉冲动画 + 内部波形 3 根竖线律动 | 单击 → 立即停止录音并开始识别 |
| S2 思考中 | ASR 识别结束，Agent 正在推理/调工具 | 琥珀色填充 + 三点 loading / 旋转图标 | 单击 → 展开抽屉显示详细进度 |
| S3 异常 | 网络断开 / API Key 未配置 / ASR 失败 | 红色边框 + 黄色警告三角，tooltip 显示具体错误 | 单击 → 展开抽屉跳转到错误提示或设置页 |

---

#### 5.2.2 P-02 抽屉式主对话面板（核心容器，P0）

**PySide6 实现要点**：
- 继承 `QWidget`，`setWindowFlags(Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint | Qt.Tool)`
- 弹出/收起动画：`QPropertyAnimation(self, b"geometry")`，目标从 `(screen_w, y, w, h)` 滑到 `(screen_w-w, y, w, h)`，250ms easeOutCubic
- 内部 5 个区块用 `QVBoxLayout` 垂直堆叠：区块之间用 `QFrame` + 1px `#E2E8F0` 水平线做分割
- 区块 2 对话记录区：`QListWidget` 为主容器，自定义 `QStyledItemDelegate` 绘制右对齐用户气泡（蓝色填充+白字+12px 圆角）和左对齐 AI 气泡（左侧 3px `#3B82F6` 竖条+`#F8FAFC` 背景+深色字）
- 区块 3 调试面板 Accordion 动画：`QParallelAnimationGroup` 同时动画 `QScrollArea` 的 maximumHeight 和 标题箭头 `QToolButton` 的 rotation（自定义 `QProxyStyle` 或 `QTransform.rotate` 自绘箭头）
- 输入区：多行 `QPlainTextEdit` 设置 `setFixedHeight(40)` + `Enter` 键事件过滤（`eventFilter` 捕获 Enter 发送，Shift+Enter 换行）
- 快捷指令 Chip：`QHBoxLayout` + 多个自定义 `ChipButton`（继承 `QPushButton`，重写 `paintEvent` 自绘 8px 圆角白底灰边、hover 主色边）
- 整体边框阴影：`QGraphicsDropShadowEffect`（blurRadius=32，color `#0F172A` alpha 0.12），注意 Frameless 模式配合 `WA_TranslucentBackground=False` 才生效

**形态参数**：
- 尺寸：宽 420px × 高 680px（可微调最大 480×780，最小 360×560，右下角设置 `QSizeGrip` 拖放缩放）
- 位置：默认从桌面右侧边缘滑入，距离右 0、上下居中；也可设置为"悬浮球上方弹出"模式（设置里切换）
- 窗口属性：Qt `FramelessWindowHint` + 默认置顶（可在 P-04 设置取消置顶）；`setAttribute(Qt.WA_ShowWithoutActivating)` + `setFocusPolicy(Qt.NoFocus)` 确保弹出时不抢当前窗口打字焦点

**完整布局 ASCII 图（从上到下 5 个区块）**：

```
╔══════════════════════════════════════════════════════╗
│  顶栏区 (H-48px)                                       │ ← 区块 1
│  ┌────────────────────────────────────────────────┐  │
│  │ 🎙 桌面语音助手        [对话][历史]  ⚙  ⊞  ✕  │  │
│  │ 品牌+标题                Tab切换   设置 调试 关 │  │
│  └────────────────────────────────────────────────┘  │
╠══════════════════════════════════════════════════════╣
│  Accordion #1 对话记录区 (H-340px 可伸缩)             │ ← 区块 2
│  ┌────────────────────────────────────────────────┐  │
│  │ ▼ 对话记录  〔今天 14:32〕                    ├─┐│
│  │ ┌────────────────────────────────────────────┐ │ ││
│  │ │   ┌──────────────────────┐ 用户气泡(右蓝)  │ │ ││
│  │ │   │找一下桌面的周报文档 📄│ ← 14:32        │ │ ││
│  │ │   └──────────────────────┘                 │ │ ││
│  │ │ ┌────────────────────────────────┐ AI气泡  │ │ ││
│  │ │ │🟦 找到 2 个文件：              │ (左浅灰)│ │ ││
│  │ │ │ 1. 周报_2026_W31.docx 📄      │         │ │ ││
│  │ │ │ 2. 周报_模板.docx 📄          │         │ │ ││
│  │ │ │ 要打开第几个？ [1][2][取消] ← 内嵌操作卡 │ │ ││
│  │ │ └────────────────────────────────┘         │ │ ││
│  │ │   ⠋⠋⠋  ← Agent thinking 三小点跳动        │ │ ││
│  │ └────────────────────────────────────────────┘ │ │
│  └───点击▲折叠为仅显示标题─────────────────────────┘─┘│
╠══════════════════════════════════════════════════════╣
│  Accordion #2 消息同步调试面板 (H-180px 默认折叠)     │ ← 区块 3
│  ┌────────────────────────────────────────────────┐  │
│  │ ▼ 消息同步调试  [12 条日志]  [🔍过滤] [🗑清]   │  │
│  │ ┌─深底终端风──────────────────────────────────┐│  │
│  │ │[14:32:05] ✔ ASR 识别结束                   ││  │
│  │ │            文本="找一下桌面的周报文档"      ││  │
│  │ │[14:32:05] ⏳ Agent_Think  48ms             ││  │
│  │ │            意图=文件搜索  置信度=0.94       ││  │
│  │ │[14:32:06] ▶ Tool:search_files              ││  │
│  │ │            params={q="周报",path="桌面"}   ││  │
│  │ │[14:32:06] ◀ Observation: 2 matches        ││  │
│  │ │[14:32:07] ✔ Agent_Answer → UI (62 字)      ││  │
│  │ └─────────────────────────────────────────────┘│  │
│  └───点击▲折叠为仅标题条──────────────────────────┘  │
╠══════════════════════════════════════════════════════╣
│  快捷指令卡片区 (H-56px 横向滚动)                     │ ← 区块 4
│  ┌────────────────────────────────────────────────┐  │
│  │[创建文件] [搜文件] [OCR截图] [开浏览器] [今热搜]│  │
│  │  ← 常用预设指令，点击即填入输入框并发送         │  │
│  └────────────────────────────────────────────────┘  │
╠══════════════════════════════════════════════════════╣
│  输入操作区 (H-96px 贴底)                             │ ← 区块 5
│  ┌────────────────────────────────────────────────┐  │
│  │ [按住说话 ████]  [🎤切换按键模式]  [📎附件]    │  │
│  │  ────────────────────────────────────────────  │  │
│  │ |⌨ 输入文字指令 (Enter发送 / Shift+Enter换行) │  │
│  │ ─────────────────────────────────────────────  │  │
│  │               [📋粘贴] [🔊TTS开/关] [↑发送]   │  │
│  └────────────────────────────────────────────────┘  │
╚══════════════════════════════════════════════════════╝
```

**区块 1 — 顶栏组件明细**：
| 组件 | 说明 | 交互 |
|------|------|------|
| 左侧品牌图标 + 标题 | "🎙 桌面语音助手" | 双击回到对话 Tab 顶部 |
| Tab 切换：对话 / 历史 | 选中态：主色下划线 2px | 点击切换区块 2 的内容 |
| 右侧图标按钮 1（⚙） | 齿轮 SVG，18×18，tooltip="设置" | 点击 → 弹出 P-04 设置窗口 |
| 右侧图标按钮 2（⊞） | 调试开关 SVG，tooltip="显示/隐藏调试面板" | 点击 → 强制切换区块 3 的折叠状态 |
| 右侧图标按钮 3（✕） | 关闭 SVG，tooltip="收起面板" | 点击 → 抽屉滑出隐藏（不退出程序） |

**区块 2 — 对话记录区（Accordion 折叠抽屉，展开态见上）**：
- 折叠方式：标题栏左侧 ▼ 箭头点击 → 箭头旋转为 ▲ + 内容区高度动画收起为 0，仅保留 32px 标题条（显示"对话记录 · 3 条未读"）
- 气泡规范：用户气泡**右对齐**，蓝色填充白字圆角 12px + 时间戳 12px 灰字在气泡右上角；AI 气泡**左对齐**，浅灰 Slate-50 底深色字，左侧带 3px 主色竖条区分
- AI 内嵌操作卡：当回复需要用户选择时（如打开第几个 / 删除确认），在气泡底部嵌入灰色操作按钮，点击即自动发送对应文字指令
- 消息自动滚动：新消息到底部自动 scroll；若用户手动上滑看历史，新消息仅显示"↓ 有新消息"浮标，点击才跳到底

**区块 3 — 消息同步调试面板（Accordion 折叠抽屉，默认折叠）**：
- 折叠方式：同区块 2，上下箭头 + 高度过渡
- 视觉：深色 Slate-900 背景 + 等宽字体 + 彩色语义点（✔绿 / ⏳橙 / ▶蓝 / ◀紫 / ✖红）
- 内容结构：每条日志 = 时间戳 + 语义图标 + 阶段名 + 详情缩进子行（对齐格式固定，类终端日志）
- 过滤与操作：右上角"过滤"下拉框 → 仅看 ASR / 仅看 Tool / 仅看错误；"清"清空当前轮日志
- 阶段枚举（一条完整指令必然经历的步骤）：
  1. `ASR_START → ASR_END` 语音识别始末
  2. `Agent_Think` 首次推理 + 意图识别 + 置信度
  3. `Tool:<name>` 工具调用 + params JSON（可展开查看完整入参）
  4. `Observation` 工具返回结果摘要
  5. `Agent_Answer` 最终回复 + 字数/耗时
  6. `TTS_PLAY` 语音播报开始/结束
  7. `ERROR_*` 任意环节失败 + 错误码 + 重试信息

**区块 4 — 快捷指令卡片区（横向滚动）**：
- 预设 6~8 张 Chip 标签（8px 圆角，白底灰边，hover 主色边）：创建文件、搜文件、OCR 截图、开浏览器、今日热搜、音量静音、锁屏、新建日程
- 支持自定义：P-04 设置中可增删改
- 点击行为：等价于输入对应文字 + 自动 Enter 发送

**区块 5 — 输入操作区（三行布局）**：
| 行 | 组件 | 细节 |
|----|------|------|
| 第一行 | [按住说话] 大按钮（占 60% 宽度） | 默认绿色"按住说话"；按住→变红+录音S1态；松开→停止识别；支持切换为"点击开始/点击结束"模式 |
| 第一行 | [🎤 切换模式] 小按钮 | 按住说话 ↔ 点击录音 ↔ 仅文字，三态循环切换 |
| 第一行 | [📎 附件] | 选择本地文件 → 直接把文件路径/内容传给 Agent（"帮我总结这个 PDF"） |
| 第二行 | 多行文本框 | 高 40px，可滚动，placeholder="⌨ 输入文字指令 (Enter发送 / Shift+Enter换行)" |
| 第三行 | 左：[📋 粘贴剪贴板] 识别粘贴的图片/文本；中：[🔊 TTS 开关] 图标切换带勾选提示；右：[↑ 发送] 主色圆形按钮（也可键盘 Enter 触发） |

---

#### 5.2.3 P-03 消息同步调试面板（Accordion 嵌入 P-02，P0/P1）

**PySide6 实现要点**：
- 容器：`QScrollArea` + 内部 `QPlainTextEdit`（设置为只读），背景色 `#0F172A`、文字 `#E2E8F0`、Consolas 13px 等宽字体
- 语法高亮：自定义 `QSyntaxHighlighter` 子类，用正则匹配关键字上色：
  - 时间戳 `[hh:mm:ss]` → 灰色 `#94A3B8`
  - `ASR_*` / `Agent_Think` → 橙色 `#F59E0B`
  - `Tool:*` / `▶` → 蓝色 `#3B82F6`
  - `Observation` / `◀` → 紫色 `#8B5CF6`
  - `✔` 成功 → 绿色 `#10B981`；`✖` 错误 → 红色 `#EF4444`
- 实时追加日志：通过对话管理器 `append_debug_log(level, text)` 方法，内部用 `QMetaObject.invokeMethod` 跨线程安全追加（LangGraph 回调在 worker 线程，不能直接刷 UI）
- 过滤下拉框：`QComboBox` + `QSortFilterProxyModel` 过滤当前显示的日志行；清空按钮调用 `clear()`
- Accordion 折叠控件：自定义 `AccordionSection` 类（QWidget 子类），包含一个 32px 标题条（QLabel 文字 + QToolButton 箭头）+ QScrollArea 内容区；箭头 180° 旋转用 `QVariantAnimation` 驱动 valueChanged 信号更新 `QToolButton.setArrowType(Qt.DownArrow/UpArrow)`

---

#### 5.2.4 P-04 设置中心窗口（独立标准窗口，P0）

**PySide6 实现要点**：
- 继承 `QMainWindow`，保留 Windows 原生标题栏（不要 Frameless，让用户可以拖到任意位置、最大化）
- 布局：`setCentralWidget(QWidget)` 内部用 `QHBoxLayout` 左右两列：左列 180px 左侧导航 + 右列 460px 设置表单卡片
- **左侧 Accordion 分类导航**：5 个 `AccordionSection`（通用设置、语音设置、快捷键、AI 与密钥、数据与隐私），每个分类下是 `QListWidget` 无子项（或 2~3 个子项），点击切换右侧内容
- **右侧表单卡片**：`QStackedWidget` 对应 5 个分类页面，每页内部用 `QFormLayout`（标签 + 控件一行一行排列），控件类型：
  - 开关：自定义 `SwitchToggle`（继承 QAbstractButton，重写 paintEvent 自绘 iOS 风格圆角开关）
  - 输入框：`QLineEdit`，密码字段设置 `setEchoMode(QLineEdit.Password)` + 右侧小眼睛按钮切换显示
  - 下拉框：`QComboBox`（ASR 引擎 / TTS 音色 / Qwen 模型系列）
  - 滑块：`QSlider`（TTS 音量/语速 / 唤醒词灵敏度）
  - 快捷键捕获：自定义 `ShortcutCaptureLineEdit`，点击后进入"捕获模式"，监听 `keyPressEvent` 记录组合键，冲突检测用 `QKeySequence` 比对
- 底部操作栏：`QDialogButtonBox` 风格自定义，左"恢复默认"、右"保存"（主色按钮）

**窗口规格**：宽 640px × 高 520px，Windows 原生标题栏 + 最小/最大化/关闭按钮，模态显示在主面板之上（`setWindowModality(Qt.ApplicationModal)`）。

**布局**：左侧 180px 分类导航（Accordion 上下箭头折叠分类）+ 右侧 460px 设置表单卡片。

```
╔═════════════════════════════════════════════════════╗
│ 设置                                                 │
╠══════════════╦══════════════════════════════════════╣
│  ▼ 通用设置   ║ ┌────────────────────────────────┐ ║
│  ├ 外观主题    ║ │  通用                          │ ║
│  ├ 启动选项    ║ │  ✔ 开机自启动                  │ ║
│  └ 语言区域    ║ │  ✔ 启动后最小化到悬浮球        │ ║
│  ▼ 语音设置   ║ │  抽屉位置: ○右侧  ○悬浮球上方  │ ║
│  ├ 唤醒词      ║ └────────────────────────────────┘ ║
│  ├ ASR引擎    ║ ┌────────────────────────────────┐ ║
│  └ TTS 播报    ║ │  语音                          │ ║
│  ▼ 快捷键     ║ │  唤醒词: [小助手          ] 🖍  │ ║
│  └ 全部快捷键  ║ │  ✔ 启用唤醒词（省电:关闭）     │ ║
│  ▼ AI 与密钥  ║ │  ASR: [阿里云DashScope ▼]      │ ║
│  ├ Qwen配置   ║ │  TTS 音色: [晓晓女声 ▼] [试听] │ ║
│  └ 工具权限    ║ └────────────────────────────────┘ ║
│  ▼ 数据与隐私 ║ ┌────────────────────────────────┐ ║
│  └ 历史管理    ║ │  AI 密钥（加密本地保存）       │ ║
│               ║ │  QWEN_API_KEY: [●●●●●●●● 编辑]│ ║
│               ║ │  DASHSCOPE_KEY: [●●●●●●●● 编辑]│ ║
│               ║ └────────────────────────────────┘ ║
│               ║    [恢复默认]              [保存] ║
╚══════════════╩══════════════════════════════════════╝
```

**设置分类清单**：
| 分类 | 子设置项 |
|------|----------|
| 通用设置 | 开机自启、启动后是否最小化、抽屉弹出位置、主面板尺寸、窗口置顶开关、深色模式开关（预留） |
| 语音设置 | 唤醒词文本编辑 + 灵敏度滑块、ASR 引擎切换（DashScope / FunASR 离线）、麦克风设备选择、TTS 开关、TTS 音色 + 语速音量、试听按钮 |
| 快捷键 | 唤起/收起抽屉、按住说话、开始/结束录音、快速截图 OCR — 每项可点击捕获新快捷键，冲突检测红色提示 |
| AI 与密钥 | Qwen 模型系列下拉（plus/max/long）、QWEN_API_KEY 密码框编辑、DashScope ASR Key、调用超时设置、文件操作根目录白名单（允许/阻止的目录） |
| 数据与隐私 | 对话历史保留天数（30/90/永久）、一键清空历史、快捷指令自定义列表、危险操作确认等级（低/中/高） |

---

---

#### 5.2.5 P-05 历史会话区（P-02 内嵌 TabWidget 页，P1）

**PySide6 实现要点**：
- P-02 顶栏 Tab 切换用 `QTabWidget`（`DocumentMode=True` + `setElideMode(Qt.ElideRight)`），共 2 页：Tab1 "对话"、Tab2 "历史"
- 历史页内部 `QHBoxLayout` 左右两列：
  - 左列 100px：`QListWidget` 日期分组（今天/昨天/本周/更早），右侧画数量徽标（`QStyledItemDelegate` 重写 paint 在 itemRect 右上角绘制红色椭圆 `QPainter.drawEllipse()` + 白色数字）
  - 右列 320px：`QListWidget` 会话摘要卡片，自定义 `HistoryCardDelegate` 绘制首句指令、AI 回复首行、时间戳、工具图标小徽章
- 详情回放：点击卡片后弹出 `QDialog`（非模态），内嵌 P-02 同款气泡 `QListWidget` + 调试面板 `QPlainTextEdit`，从 SQLite 加载对应 thread_id 的历史消息与 LangGraph Checkpoint 记录逐条回放
- 顶部搜索：`QLineEdit` + `QSortFilterProxyModel` 按用户指令关键词过滤

**功能要点**：
- Tab 切换后取代区块 2 对话记录区；区块 3 调试面板可同步切换为"该历史指令对应日志回放"
- 左侧 100px：日期分组列表（今天 / 昨天 / 本周 / 更早 + 数量徽标）
- 右侧 320px：会话摘要卡片，每卡 = 首句用户指令摘要 + AI 回复首行 + 时间戳 + 使用了哪些工具的小图标
- 点击卡片 → 右侧滑出完整对话 + 调试日志完整回放（可逐条点高亮观察）
- 顶部搜索框：按关键词过滤历史对话

---

#### 5.2.6 P-06 系统托盘菜单（P1）

**PySide6 实现要点**：
- `QSystemTrayIcon` + 自定义图标（4 种状态对应 P-01 悬浮球：待机/录音/思考/错误，`setIcon(QIcon(...))` 切换）
- 关联 `QMenu` 作为右键菜单；托盘图标 `activated` 信号：`Trigger`（单击）→ 显示 P-02、`Context`（右键）→ 弹出菜单
- 关闭行为：重写 `QApplication.setQuitOnLastWindowClosed(False)`，所有窗口关闭后仍保留托盘进程；必须菜单「退出」才 `qApp.quit()`
- 二次确认退出：点击「退出桌面语音助手」时先 `exec()` 一个小 `QMessageBox.question()` 确认

**菜单结构（从上到下）**：
1. 「显示主面板」 → 展开 P-02
2. 「立即录音」 → 直接进入 S1 录音态
3. 「开关」 → 子菜单：☑ 唤醒词识别 / ☑ TTS 自动播报 / ☐ 显示调试面板（全部可勾选，状态同步到 P-04 设置）
4. 「设置」 → P-04
5. 「检查更新」 → 占位（后续实现）
6. 「退出桌面语音助手」 → 二次确认后彻底退出

---

#### 5.2.7 P-07 高危操作确认弹窗（P0，强制）

**PySide6 实现要点**：
- 继承 `QDialog`，`setWindowModality(Qt.ApplicationModal)` 阻塞所有其他窗口交互
- 布局：`QVBoxLayout` 垂直堆叠，分 4 部分：① 警告标题区 ② 清单滚动区 ③ 确认输入区 ④ 底部操作按钮
- 清单滚动区：`QScrollArea` 内嵌 `QListWidget`，每行前有 `QCheckBox`（可勾选保留个别项不执行），永久删除项行前加 `⚠` 红色图标 `QDecorationRole`
- DELETE 输入框：`QLineEdit` 设置 `placeholderText="请输入 DELETE 确认"`，只有输入全大写 DELETE 并匹配后，「确认删除」按钮才 `setEnabled(True)`（默认禁用灰）
- 语音确认：按钮旁 `QPushButton` + 麦克风图标，点击后启动 10s ASR 录音，识别成功包含「确认执行删除」即自动通过
- 倒计时：纯回收站删除（无永久项）简化模式，「确认」按钮旁显示 5s 倒计时 `QLabel("5s 后可确认")` 倒计时结束才能点（防手抖）
- Esc 键：重写 `keyPressEvent` 捕获 `Qt.Key_Escape` → 等同「取消」，自动取消操作

**触发条件**：由 LangGraph 的「高危确认条件边」判断指令属于高危后，发送 Qt 信号到对话管理器 → 对话管理器 `QMetaObject.invokeMethod` 在 UI 线程 `exec()` 此 QDialog，用户结果写回 LangGraph GraphState 恢复状态图执行。

**布局样式**（PySide6 QDialog 原生控件实现）：

```
               ┌───────────────────────────────────────┐
               │ ⚠  高危操作确认                  [_][×]│
               ├───────────────────────────────────────┤
               │  你即将执行以下删除操作：               │
               │  ┌─ 清单区 (可滚动 高 180) ──────────┐ │
               │  │ ☒ 回收站删除 共 4 个文件  312MB   │ │
               │  │   1. D:\Downloads\a.zip           │ │
               │  │   2. D:\Downloads\b.zip           │ │
               │  │   3. D:\Downloads\c.zip           │ │
               │  │   4. ⚠ 永久删除(不可恢复): d.zip  │ │
               │  └───────────────────────────────────┘ │
               │                                         │
               │  为确认本次操作，请在下方输入：DELETE   │
               │  ┌───────────────────────────────────┐ │
               │  │ [ DELETE                        ] │ │
               │  └───────────────────────────────────┘ │
               │  或语音朗读确认："确认执行删除操作"     │
               │           [🎙 语音确认]                 │
               ├───────────────────────────────────────┤
               │         [取消]           [确认删除 🔴] │
               └───────────────────────────────────────┘
```

**规则**：
- 只要清单中包含"永久删除"，必须启用文字输入框验证（DELETE 全大写）；纯回收站删除可改为仅点按钮 + 倒计时 5 秒（防止手抖）
- 支持语音确认：点 [🎙 语音确认] 开始 10s 录音，识别匹配"确认执行 XXX"即视为通过
- 取消方式：Esc 键 / 取消按钮 / 关闭按钮 / 30 秒无操作自动超时关闭并取消

---

### 5.3 窗体状态流转图（全部为桌面原生控件，无 Web 跳转）

```
        程序启动 (QApplication.exec)
           │
           ▼
 ┌────────────────────┐
 │ P-01 悬浮球(S0待机) │ ←──────────┐  Qt.WindowStaysOnTopHint
 │ (QWidget 无边框透明)│            │
 └──┬─────────┬───────┘            │关闭抽屉动画
    │单击     │唤醒词/快捷键录音    │QPropertyAnimation隐藏
    ▼         ▼                    │
 ┌────────────────┐   指令执行完毕   │
 │ P-02 抽屉展开  │ ────────────────┘
 │ (QWidget 侧滑) │
 │ 5区块垂直布局   │
 │ 内嵌P-03(调试)/P-05(历史Tab)
 └──┬─────────┬───┘
    │点击⚙    │高危操作条件边触发(Qt信号)
    ▼         ▼
 ┌─────────────┐  ┌───────────────┐
 │ P-04 设置窗 │  │ P-07 确认弹窗  │──确认/取消→写回GraphState→恢复LangGraph
 │(QMainWindow)│  │ (QDialog 模态)│      继续执行工具节点
 └─────────────┘  └───────────────┘
    │右键托盘图标
    ▼
 ┌──────────────┐
 │ P-06 托盘菜单│
 │ QSystemTrayIcon + QMenu
 └──────────────┘
   点击「退出」→ qApp.quit() 关闭全部
```

> **跨线程通信强调**：LangGraph 在 QThread worker 线程运行，所有 UI 控件操作（弹窗、显示气泡、追加日志）**必须**通过 `pyqtSignal` 信号槽或 `QMetaObject.invokeMethod(..., Qt.QueuedConnection)` 投递到 UI 主线程执行，禁止在 LangGraph 回调线程直接 new / show Qt 控件（会触发 Qt ASSERT 崩溃）。

---

## 六、核心交互流程与关键用例

### 6.1 核心用例总览（界面形态已在第五章详细定义）

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│                    [桌面区域]                            │
│                                                         │
│                               ┌───────┐                 │
│                               │ ●●●   │ ← 悬浮球         │
│                               │ (动效)│   正常态: 静态图标│
│                               └───┬───┘   录音态: 波形动效│
│                                   │ 点击展开抽屉          │
└───────────────────────────────────┼─────────────────────┘
                                    ▼
              ┌─────────────────────────────────────┐
              │  🎙 桌面语音助手    [−][×]  [调试⊞] │ ← 顶栏
              ├─────────────────────────────────────┤
              │  ▼ 对话记录 (折叠箭头点击展开)       │ ← accordion 抽屉
              │  ┌─────────────────────────────────┐│
              │  │  用户: 找一下桌面的周报文档     ││ ← 用户气泡 (右)
              │  │  🤖: 找到 2 个文件:            ││
              │  │      1. 周报_2026_W31.docx    ││ ← AI 气泡 (左)
              │  │      2. 周报_模板.docx         ││
              │  │      要打开第几个？            ││
              │  └─────────────────────────────────┘│
              ├─────────────────────────────────────┤
              │  ▼ 消息同步调试面板 (默认折叠)     │ │ ← accordion
              │  ┌─────────────────────────────────┐│
              │  │  [12:03:01] ASR → "找一下桌面…" ││ ← 可视化调试
              │  │  [12:03:01] Agent-Thought:      ││   每步流转状态
              │  │    用户意图: 文件搜索           ││
              │  │  [12:03:02] Tool: search_files  ││
              │  │    params={q="周报", path="桌面"}││
              │  │  [12:03:02] Observation: 2 结果 ││
              │  │  [12:03:03] Agent-Answer → UI  ││
              │  └─────────────────────────────────┘│
              ├─────────────────────────────────────┤
              │ [按住说话] [🎤快捷]  [⌨ 文字输入]  │ │ ← 输入区
              └─────────────────────────────────────┘
```

### 5.2 关键用例 (Use Case)

#### 用例 1: 语音创建文件
```
触发: 用户按 Ctrl+Alt+V 或 说"小助手"
用户: "在桌面新建一个 markdown 文件，标题是项目启动计划，帮我写一个简单的大纲，包含背景、目标、里程碑三个部分"
↓
ASR 转文字 → Agent 理解:
  意图 = 创建文件 + 写入内容
  Tool 1: 写文件 (路径=~/Desktop/项目启动计划.md, 内容=LLM 生成的大纲)
↓
工具执行 → 创建成功
↓
Agent 回复 + TTS: "已经帮你在桌面创建了《项目启动计划.md》并写好大钢，要打开看看吗？"
↓
用户: "打开"
↓
Tool: 系统默认程序打开该文件
```

#### 用例 2: 删除文件（强制安全确认）
```
用户: "删掉下载文件夹里所有的 zip 压缩包"
↓
Agent 识别 → 高危操作，先执行查询列出清单:
Tool: search_files(path=~/Downloads, ext=.zip) → 返回 5 个文件，共 1.2GB
↓
Agent 反问（必须确认）:
  "下载目录里有 5 个 zip 文件（共 1.2GB）：
   1. a.zip  2. b.zip  ...
   确认要全部删除吗？回答确认删除 / 取消 / 保留第几个"
↓
用户: "确认删除，但保留第 3 个"
↓
Agent 修正后执行删除 → 移入回收站 4 个
↓
回复: "已删除 4 个，保留 XXX.zip，需要的话可以在回收站找回"
```

#### 用例 3: OCR 识别截图
```
用户: [按截图快捷键后拖选区域] "识别刚才这张截图的文字，保存到剪贴板"
↓
Agent: Tool = OCR(img=剪贴板最新图片) → 返回文字
↓
Agent: Tool = write_clipboard(text=...)
↓
回复: "识别到 3 段共 128 字，已经复制到剪贴板，内容是：......"
```

#### 用例 4: 搜索今日资讯
```
用户: "今天有什么科技圈的新闻？用语音给我播报一下"
↓
Agent: Tool = search_news(category=tech, date=today) → 返回 8 条
↓
Agent: LLM 汇总成简报 → TTS 朗读 3-5 条要点
↓
中途用户: "跳过这条，下一条" / "这条详细说说"
↓
Agent 响应动态调整播报内容
```

---

## 七、非功能需求

| 维度 | 指标 |
|------|------|
| **响应速度** | 简单指令（打开网页、创建空文件）≤ 2s 给出反馈<br>复杂指令（搜索+摘要）首字节 ≤ 5s，进度可视化 |
| **准确率** | ASR 中文普通话 ≥ 95%；文件操作误执行率 = 0（高危必须二次确认） |
| **资源占用** | 空闲时 CPU < 2%，内存 < 200MB；录音+推理峰值 < 500MB |
| **隐私安全** | 删除操作默认回收站；本地文件不上传云端（除 LLM 接口必要文本）；可配置纯离线模式 |
| **可扩展性** | 新增功能 = 新增一个 Tool 类 + 在工具注册表注册，无需修改核心架构 |
| **稳定性** | 工具调用失败自动重试 2 次，仍失败则提示用户而非崩溃 |

---

## 八、开发里程碑（建议节奏，全程 LangGraph 建图 + PySide6 桌面原生）

| 阶段 | 周期 (估) | 核心交付物 |
|------|-----------|------------|
| **M1: 原型验证（文字 Agent 先跑通）** | 1-2 周 | **LangGraph 建 ReAct 状态图**（agent 节点 + tools 节点 + 循环条件边）+ LangChain 封装 3 个 Tool（create_file/search_files/open_browser）+ Qwen 模型；PySide6 临时极简窗口（纯 QTextEdit 输入输出，不做 UI 美化）；验证目标：文字指令 → LangGraph 调工具 → 拿结果 |
| **M2: 语音接入 + 桌面 UI 雏形** | 1 周 | + ASR DashScope、TTS Edge-TTS；**P-01 桌面悬浮球** QWidget 自绘实现（4 状态切换）+ **P-02 抽屉式主面板** QWidget 滑入滑出（5 区块雏形，先不做 Accordion 动画）；LangGraph 加入 `SQLiteCheckpointSaver` 验证断点恢复 |
| **M3: 文件工具集 + 高危确认条件边** | 2 周 | 文件 CRUD + Whoosh 全文索引 + PaddleOCR + 文档摘要；**LangGraph 新增高危确认条件边** → 触发 **P-07 QDialog 原生高危弹窗**（DELETE 输入 + 清单 + 语音确认）；验证关闭程序重开后"待确认"状态可恢复 |
| **M4: 网络工具 + 资讯聚合** | 1 周 | 浏览器 webbrowser 打开 + 新闻热搜爬取 + 搜索引擎摘要；LangGraph 可选启用 `ToolNode(parallel=True)` 多工具并行调用 |
| **M5: UI 打磨（Accordion + 调试面板 + 设置）** | 1 周 | **P-03 消息同步调试面板**（QSyntaxHighlighter 上色 + LangGraph `astream_events()` 事件订阅）+ **P-04 设置中心 QMainWindow**（左侧 Accordion 分类 + 右侧表单 + 快捷键捕获）+ **P-05 历史会话 Tab** + **P-06 QSystemTrayIcon 托盘菜单**；QPropertyAnimation 实现所有 Accordion 上下箭头折叠动画 |
| **M6: PyInstaller 打包 + 扩展工具** | 1 周 | 单 exe 打包（PaddleOCR/LangChain 依赖处理验证）；按需补 Tool（系统音量/应用启动/定时提醒/翻译/代码助手）；**可选**：LangGraph 升级为多 Agent 路由架构（supervisor 节点 + 文件 Agent / 资讯 Agent / 系统 Agent 子图） |

---

## 九、待确认与后续讨论点

> 写文档时发现这些细节后面需要你拍板（不拍板也可以用默认值直接开干 M1）：

1. **唤醒词名称**：你希望叫什么？小助手 / 小白 / 小智 / 自定义？**默认=小助手**
2. **默认文件操作根目录**：默认允许操作整个磁盘？还是限定桌面+文档+下载？**默认=整个磁盘（高危操作都会弹 P-07 二次确认）**
3. **LLM 模型选择**：Qwen 用哪个系列？qwen-plus / qwen-max / qwen-long？**默认=qwen-plus（性价比最高）**
4. **API Key 配置方式**：首次启动走 **P-04 设置中心窗口** 让用户填写（加密存 SQLite），还是写死在 `config.ini`？**默认=P-04 设置中心填写**
5. **P-03 调试面板默认可见性**：默认折叠隐藏（普通用户体验），还是默认展开（开发阶段便于看执行流）？**默认=开发期默认展开、发布版默认折叠**
6. **窗体与面板风格补充确认**（均为 PySide6 Qt 原生控件，非 Web）：
   - 主抽屉 P-02 弹出方向：默认右侧滑出？还是悬浮球正上方弹出？**默认=右侧滑出**
   - P-02 主面板尺寸：默认 420×680 是否合适？还是要更大/更窄？**默认=420×680**
   - 配色方案：当前 AI-Native 科技蓝 `#3B82F6` 是否喜欢？要不要换色调（紫 `#8B5CF6` / 绿 `#10B981` / 橙 `#F59E0B`）？**默认=科技蓝**
7. **LangGraph Agent 架构选型**（你明确提了双框架配合，这里选图的复杂度）：
   - **方案 A（推荐，M1~M6）**：**单 ReAct Agent 状态图**，所有工具共用一张 StateGraph（agent→tools→条件边循环），简单稳定、调试方便
   - **方案 B（M6 后期升级）**：**多 Agent Supervisor 路由图**，一个顶层 supervisor 节点分发到 `FileAgent / NewsAgent / SystemAgent` 三个子 StateGraph，扩展性更强但复杂度高
   - **默认=方案 A，M6 后视情况升级到 B**

---

> **文档状态**: v1.2 已对齐两项核心澄清：
> 1. **非 Web**：第 5 章 UI/UX 全部明确为 PySide6 Qt 原生 QWidget/QMainWindow/QDialog/QSystemTrayIcon 实现，每个面板追加「PySide6 实现要点」小节，写明具体控件类、动画方案、跨线程通信规则；
> 2. **LangChain + LangGraph 双框架**：第 3 章架构图拆分为 LangChain（能力封装）+ LangGraph（流程/状态/Checkpoint）两层，第 4 章技术选型明确两框架版本与分工口诀，第 3.3 数据流改为 LangGraph 节点条件边流转，第 8 章里程碑全程以 LangGraph 建图为主线。
> 审阅确认后即可进入技术方案细化或直接开干 M1 原型。
